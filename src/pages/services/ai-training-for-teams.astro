---
import Base from '../../layouts/Base.astro';
import SocialProofIsland from '../../components/islands/SocialProofIsland';
import { HowItWorksIsland } from '../../components/islands/HowItWorksIsland';
import { StakesCTAPulse } from '../../components/islands/StakesCTAPulse';
import { StaggerHeading } from '../../components/islands/StaggerHeading';
import { TIDYCAL_URL } from '../../config';
import { trainingTestimonials } from '../../data/social-proof';

const outcomes = [
  {
    icon: 'ph-wrench',
    title: 'Built on real work',
    desc: "Every workshop is built around your team's actual tasks — the tools they'll use tomorrow, the problems they're dealing with today.",
  },
  {
    icon: 'ph-play-circle',
    title: 'Learn by doing',
    desc: "People learn by doing, not by watching slides. Your team practises with AI on their actual tasks, not hypothetical examples.",
  },
  {
    icon: 'ph-trend-up',
    title: 'Behavioural change, not certificates',
    desc: "Gary tracks daily tool usage and behavioural change, not completion rates. The goal is adoption, not attendance.",
  },
];

const scenarios = [
  {
    label: 'Teams told to "use AI"',
    desc: "Given the mandate but no guidance on where to start or what tools to use",
  },
  {
    label: 'Knowledge workers',
    desc: "Sales, marketing, and operations teams doing knowledge work every day",
  },
  {
    label: 'Groups of 5 to 50',
    desc: "Workshops scale from small teams to full departments",
  },
];
---

<Base title="AI Training for Teams — Consultates" description="Hands-on AI workshops built around your team's actual work. Design thinking approach — people learn by doing.">

  <!-- Hero: dark -->
  <section class="svc-hero bg-dots-dark">
    <div class="svc-hero__content">
      <h1 class="text-hero font-serif" style="color: #FFFFFF; max-width: 820px; margin: 0 auto;">AI Training for Teams</h1>
      <p class="text-body-lg mt-4" style="color: var(--hero-muted); max-width: 600px; margin: 0 auto;">
        Hands-on workshops built around your team's actual work. People learn by doing, not by watching slides.
      </p>
      <div class="mt-8">
        <a href={TIDYCAL_URL} target="_blank" rel="noopener noreferrer" class="btn-alive btn-alive--lg btn-alive--on-dark">Book a Free Call</a>
      </div>
    </div>
  </section>

  <!-- What You Get: 3-card grid with icons -->
  <section class="svc-section">
    <div class="svc-container">
      <h2 class="text-section mb-8">What you get</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6" data-stagger>
        {outcomes.map((item) => (
          <div class="svc-card" data-reveal>
            <div class="svc-card__header">
              <div class="svc-icon-circle">
                <i class={`ph-duotone ${item.icon}`}></i>
              </div>
              <h3 class="text-sub font-serif">{item.title}</h3>
            </div>
            <p class="text-body text-muted-foreground">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Who It's For -->
  <section class="svc-section bg-secondary">
    <div class="svc-container">
      <h2 class="text-section mb-8">Who it's for</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6" data-stagger>
        {scenarios.map((s) => (
          <div class="svc-card svc-card--flat" data-reveal>
            <h3 class="text-sub font-serif mb-2">{s.label}</h3>
            <p class="text-body text-muted-foreground">{s.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Testimonials: carousel -->
  <section class="svc-section">
    <div class="svc-container">
      <h2 class="text-section mb-10">What clients say</h2>
      <SocialProofIsland client:visible quotes={trainingTestimonials} linkedinUrl="https://www.linkedin.com/in/garyctate/details/recommendations/" />
    </div>
  </section>

  <!-- How It Works -->
  <section class="svc-section bg-secondary">
    <div class="svc-container">
      <h2 class="text-section mb-12">How it works</h2>
      <HowItWorksIsland
        client:visible
        steps={[
          { number: '01', title: 'We talk', description: 'Tell me about your team, their roles, and where AI could help most.' },
          { number: '02', title: 'You get a plan', description: 'A workshop built around your team\'s actual tasks, tools, and workflows.' },
          { number: '03', title: 'Your team ships', description: 'Within days, your team is building automations on their actual work. Measured by adoption, not attendance.' },
        ]}
        showCta={false}
      />
    </div>
  </section>

  <!-- Dark CTA -->
  <section class="bg-dots-dark" style="background: #0D1117; position: relative; overflow: hidden;">
    <div class="glow glow-dark corner-marks max-w-[800px] mx-auto text-center"
         style="padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem); --corner-color: rgba(139,108,199,0.4);">
      <StaggerHeading client:visible text="Ready to <em>upskill</em> your team?" className="text-section" style={{ color: '#FFFFFF' }} />
      <div class="mt-8">
        <StakesCTAPulse client:visible href={TIDYCAL_URL} text="Book a Free Call" />
      </div>
    </div>
  </section>
</Base>

<style>
  .svc-hero {
    background: var(--hero-bg, #0D1117);
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(3rem, 6vh, 5rem) var(--content-px);
  }

  .svc-hero::before {
    content: '';
    position: absolute;
    top: 40%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 800px;
    height: 500px;
    background: radial-gradient(ellipse, rgba(92,59,156,0.25) 0%, rgba(92,59,156,0.10) 40%, transparent 70%);
    pointer-events: none;
    z-index: 1;
  }

  /* overlay removed — was killing glow+grid visibility */
  .svc-hero__glow {
    display: none;
    background: var(--hero-bg, #0D1117);
    opacity: 0.5;
    pointer-events: none;
  }

  .svc-hero__content {
    position: relative;
    z-index: 2;
    max-width: var(--content-max, 1140px);
    width: 100%;
    text-align: center;
  }

  .svc-section {
    padding: clamp(4rem, 8vw, 8rem) var(--content-px);
  }

  .svc-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .svc-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--card-radius);
    padding: var(--card-padding);
    border-top: 3px solid var(--primary);
  }

  .svc-card--flat {
    border-top: 1px solid var(--border);
    background: var(--card);
  }

  .svc-card__header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .svc-icon-circle {
    width: 48px;
    height: 48px;
    min-width: 48px;
    border-radius: 50%;
    background: var(--secondary);
    border: 2px solid var(--primary);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    color: var(--primary);
    /* margin removed — .svc-card__header flex handles spacing */
  }


  [data-reveal] {
    opacity: 0;
    transition: opacity 0.6s ease-out;
  }

  [data-reveal].is-visible {
    opacity: 1;
  }

  @media (prefers-reduced-motion: reduce) {
    [data-reveal] {
      opacity: 1;
      transition: none;
    }
  }
</style>

<script>
  requestAnimationFrame(() => {
    const items = document.querySelectorAll('[data-reveal]');
    if (!items.length) return;
    let staggerIndex = 0;
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const el = entry.target as HTMLElement;
            const delay = staggerIndex * 150;
            staggerIndex++;
            setTimeout(() => el.classList.add('is-visible'), delay);
            observer.unobserve(el);
          }
        });
      },
      { threshold: 0.15 }
    );
    items.forEach((item) => observer.observe(item));
  });
</script>
