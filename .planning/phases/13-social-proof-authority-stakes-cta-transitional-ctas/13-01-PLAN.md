---
phase: 13-social-proof-authority-stakes-cta-transitional-ctas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/SocialProofSection.astro
  - src/components/sections/WhyConsultatesSection.astro
  - src/components/islands/AuthorityCardsIsland.tsx
autonomous: true
requirements: [RDES-34, RDES-35, RDES-36, RDES-37, RDES-38]

must_haves:
  truths:
    - "Social proof section shows a 72x72px circle with FW initials and enlarged quote text"
    - "Authority section renders three cards with border, radius, padding, hover accent, and stat callouts"
    - "Authority cards stagger-reveal L-to-R on viewport entry"
  artifacts:
    - path: "src/components/sections/SocialProofSection.astro"
      provides: "Photo placeholder circle + enlarged quote"
      contains: "72"
    - path: "src/components/islands/AuthorityCardsIsland.tsx"
      provides: "Staggered authority card reveal animation"
      exports: ["AuthorityCardsIsland"]
    - path: "src/components/sections/WhyConsultatesSection.astro"
      provides: "Authority section using AuthorityCardsIsland"
      contains: "AuthorityCardsIsland"
  key_links:
    - from: "src/components/sections/WhyConsultatesSection.astro"
      to: "src/components/islands/AuthorityCardsIsland.tsx"
      via: "client:visible island import"
      pattern: "AuthorityCardsIsland.*client:visible"
---

<objective>
Redesign Social Proof (testimonial) and Authority (Why Consultates) homepage sections to match design system.

Purpose: Social proof gets a photo placeholder circle and enlarged quote. Authority columns become design system cards with stat callouts, hover accents, and staggered reveal animation via a new React island.
Output: Updated SocialProofSection.astro, new AuthorityCardsIsland.tsx, updated WhyConsultatesSection.astro
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/sections/SocialProofSection.astro
@src/components/sections/WhyConsultatesSection.astro
@src/components/islands/OfferingCardsIsland.tsx
@src/components/islands/SectionAnimator.tsx
@src/styles/global.css

<interfaces>
<!-- Design system tokens already in global.css -->
--card-padding: clamp(1.75rem, 3vw, 2.25rem);
--card-radius: 16px;
--card-shadow: 0 2px 16px rgba(20,28,116,0.06);
--card-shadow-hover: 0 8px 32px rgba(20,28,116,0.12);

<!-- Animation exports from src/lib/animations.ts -->
export function useReducedMotion(): boolean;
export const fadeUp: Variants;
export const scaleIn: Variants;
export const viewportConfig: { once: boolean; amount: number };
export function getVariant(v: Variants, reduced: boolean): Variants;

<!-- SectionAnimator props -->
interface SectionAnimatorProps {
  children: ReactNode;
  variant?: 'fadeUp' | 'stagger';
  delay?: number;
  className?: string;
  as?: 'div' | 'p' | 'h2';
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Social proof photo placeholder + quote size</name>
  <files>src/components/sections/SocialProofSection.astro</files>
  <action>
Update SocialProofSection.astro with two changes:

1. **Photo placeholder circle** — Before the attribution text (Frank Wiener name), add a 72x72px circle with his initials "FW". Implementation:
   - Flex row with the circle and name/title stacked beside it
   - Circle: `width: 72px; height: 72px; border-radius: 50%; background: var(--primary); opacity: 0.1` as outer, with initials centered in `font-sans font-semibold text-primary` text
   - Use a div with relative positioning: outer circle has `bg-primary/10` (Tailwind), inner span with initials centered via flexbox
   - Wrap circle + attribution in a flex row with `items-center gap-4 mt-6`

2. **Quote text size** — Change the quote `<p>` from `text-body-lg` to inline style `font-size: clamp(1.125rem, 2vw, 1.375rem)` to match RDES-35.

Keep existing decorative quote mark, secondary background, SectionAnimator wrapping, and layout structure. No new React island needed.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>Social proof section has 72x72px circle with "FW" initials, quote text at clamp(1.125rem, 2vw, 1.375rem), attribution in flex row with circle</done>
</task>

<task type="auto">
  <name>Task 2: Authority cards island + section update</name>
  <files>src/components/islands/AuthorityCardsIsland.tsx, src/components/sections/WhyConsultatesSection.astro</files>
  <action>
**Create `src/components/islands/AuthorityCardsIsland.tsx`:**

A simpler version of the OfferingCardsIsland pattern -- staggered whileInView reveal (NOT the full sequential state machine, since authority cards have no typewriter/CTA pulse sequence). Implementation:

- Import `motion, useInView` from framer-motion, `useReducedMotion` from `../../lib/animations`
- Define 3 card data objects inline with: `stat` (string: "20 years", "15 years", "Now"), `title` (string), `description` (string) matching existing WhyConsultatesSection copy exactly
- Card component renders:
  - Stat callout at top: `font-serif text-primary` with `font-size: clamp(1.5rem, 3vw, 2rem)` and `font-weight: 700`
  - Title: existing `text-sub text-foreground` styling
  - Description: existing `text-body text-muted-foreground mt-3` styling
- Card wrapper: `background: var(--card)`, `border: 1px solid var(--border)`, `border-radius: var(--card-radius)`, `padding: var(--card-padding)`, `box-shadow: var(--card-shadow)`, `transition: box-shadow 0.2s, border-color 0.2s`
- Hover: `box-shadow: var(--card-shadow-hover)`, `border-color: var(--primary)` -- use CSS hover (not Framer Motion whileHover) via onMouseEnter/onMouseLeave state or inline CSS `:hover` pseudo-class. Simplest: apply hover via a className and add a small CSS block in the component using a style tag, OR use inline React state for hover.
- Stagger animation: use `motion.div` with `whileInView` on each card, delays of 0s, 0.12s, 0.24s. Variant: translateY 24px -> 0, opacity 0 -> 1, duration 0.5s.
- `useInView` on container ref with `once: false, amount: 0.3` to reset when leaving viewport (same pattern as OfferingCardsIsland).
- Reduced motion: render static cards immediately (no animation, same card styling).
- Grid: `grid grid-cols-1 md:grid-cols-3 gap-[1.25rem]`
- Export as default function `AuthorityCardsIsland`.

**Update `src/components/sections/WhyConsultatesSection.astro`:**

- Remove SectionAnimator import, add AuthorityCardsIsland import
- Replace the entire `<SectionAnimator>` wrapped grid with `<AuthorityCardsIsland client:visible />`
- Keep the section wrapper, max-width container, and padding unchanged
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>Authority section renders 3 cards with border/radius/padding/shadow, stat callouts in serif purple at top, hover accent border, staggered reveal at 0s/0.12s/0.24s delays, reduced motion fallback shows static cards</done>
</task>

</tasks>

<verification>
- `npx astro check` passes with no errors
- `npx astro build` completes successfully
- Social proof section visible at `/#section-proof` with circle + enlarged quote
- Authority section visible at `/#section-why` with card styling + stagger animation
</verification>

<success_criteria>
- 72x72px circle with "FW" initials appears before attribution in social proof section
- Quote text renders at clamp(1.125rem, 2vw, 1.375rem)
- Authority cards have border, 16px radius, card padding, card shadow, hover accent border
- Stat callouts ("20 years", "15 years", "Now") render in serif purple at prominent size
- Cards stagger-reveal L-to-R with 0.12s delays
- Reduced motion shows all content immediately without animation
</success_criteria>

<output>
After completion, create `.planning/phases/13-social-proof-authority-stakes-cta-transitional-ctas/13-01-SUMMARY.md`
</output>
