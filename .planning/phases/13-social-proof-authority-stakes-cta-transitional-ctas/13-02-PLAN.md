---
phase: 13-social-proof-authority-stakes-cta-transitional-ctas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/StakesCTASection.astro
  - src/components/islands/StakesCTAPulse.tsx
  - src/components/sections/TransitionalCTAsSection.astro
  - src/components/BlogCard.astro
autonomous: true
requirements: [RDES-39, RDES-40, RDES-41, RDES-42, RDES-43, RDES-44, RDES-45]

must_haves:
  truths:
    - "Stakes CTA section has radial purple glow, corner marks, accent em, and pill-shaped button"
    - "Transitional CTAs section shows three cards with serif titles and hover accent border"
    - "Section heading reads 'Not ready to book? Start here.'"
  artifacts:
    - path: "src/components/sections/StakesCTASection.astro"
      provides: "Stakes CTA with glow, corners, em parsing"
      contains: "corner-marks"
    - path: "src/components/islands/StakesCTAPulse.tsx"
      provides: "Pill-shaped pulse CTA button"
      contains: "rounded-full"
    - path: "src/components/sections/TransitionalCTAsSection.astro"
      provides: "Three-card transitional section with heading"
      contains: "Not ready to book"
    - path: "src/components/BlogCard.astro"
      provides: "Blog card with serif title and hover accent border"
      contains: "font-serif"
  key_links:
    - from: "src/components/sections/StakesCTASection.astro"
      to: "src/styles/global.css"
      via: "CSS utility classes"
      pattern: "glow|corner-marks"
    - from: "src/components/sections/TransitionalCTAsSection.astro"
      to: "src/components/BlogCard.astro"
      via: "Astro component import"
      pattern: "BlogCard"
---

<objective>
Redesign Stakes CTA and Transitional CTAs homepage sections to match design system.

Purpose: Stakes CTA gets radial glow, corner marks, accent-highlighted em tags, and pill button. Transitional CTAs expands to 3 cards with section heading, serif titles, and hover accent borders.
Output: Updated StakesCTASection.astro, StakesCTAPulse.tsx, TransitionalCTAsSection.astro, BlogCard.astro
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/sections/StakesCTASection.astro
@src/components/islands/StakesCTAPulse.tsx
@src/components/sections/TransitionalCTAsSection.astro
@src/components/BlogCard.astro
@src/styles/global.css

<interfaces>
<!-- CSS utilities already in global.css -->
.glow { position: relative; }
.glow::before { radial-gradient purple wash, 600x600px, pointer-events none }
.glow > * { position: relative; z-index: 1; }

.corner-marks { position: relative; }
.corner-marks::before { top-left corner mark, 24x24px, --corner-color border }
.corner-marks::after { bottom-right corner mark }

<!-- StakesCTAPulse props -->
interface StakesCTAPulseProps {
  href: string;
  text: string;
}

<!-- BlogCard props -->
interface Props {
  title: string;
  date: string;
  excerpt: string;
  href: string;
}

<!-- Blog posts available -->
- youre-not-the-only-one-stuck.md
- how-safe-is-ai-really.md
- get-out-of-the-ai-cage.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stakes CTA glow + corners + em parsing + pill button</name>
  <files>src/components/sections/StakesCTASection.astro, src/components/islands/StakesCTAPulse.tsx</files>
  <action>
**Update `src/components/sections/StakesCTASection.astro`:**

1. **Add glow + corner-marks** — Add `glow corner-marks` classes to the inner `max-w-[800px]` div. The `.glow` and `.corner-marks` utilities are already defined in global.css. For corner marks on a dark background, override `--corner-color` to `rgba(139,108,199,0.4)` via inline style on the div so corners are visible against the dark bg.

2. **Parse em tags in headline** — The headline text "84% of people haven't started. The 0.04% who build with AI agents aren't waiting for you to catch up." needs `<em>` wrapping around key phrases for accent purple highlight. Wrap the stats in em tags:
   - Change the h2 content to use `<em>` around "84%" and "0.04%"
   - Style em tags: `font-style: normal; color: #8B6CC7;` (purple lifted, since this is on dark bg)
   - Add a `<style>` block in the Astro component or use inline styles on the em elements

3. **Keep existing SectionAnimator + StakesCTAPulse** — Don't change animation structure.

**Update `src/components/islands/StakesCTAPulse.tsx`:**

4. **Pill button** — Change `rounded-lg` to `rounded-full` in both the reduced-motion `<a>` and the `<motion.a>` className. This makes the CTA pill-shaped matching the hero CTA. Both occurrences (lines ~31 and ~44).
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>Stakes CTA section has radial purple glow behind content, corner marks on container, "84%" and "0.04%" highlighted in accent purple via em tags, CTA button is pill-shaped (rounded-full)</done>
</task>

<task type="auto">
  <name>Task 2: Transitional CTAs three-card layout + BlogCard serif + hover</name>
  <files>src/components/sections/TransitionalCTAsSection.astro, src/components/BlogCard.astro</files>
  <action>
**Update `src/components/BlogCard.astro`:**

1. **Serif title** — Change the h3 from `text-sub text-card-foreground` to `text-sub font-serif text-card-foreground` to render titles in IBM Plex Serif.

2. **Hover accent border** — Replace the current hover classes (`hover:-translate-y-0.5 hover:shadow-[0_4px_12px_rgba(0,0,0,0.08)]`) with design system hover treatment: `hover:border-primary hover:shadow-[var(--card-shadow-hover)]` matching offering card hover pattern. Keep the existing `border border-border rounded-xl` base. Add `transition-all duration-200` (already present).

NOTE: BlogCard is shared with the blog index page. Serif titles and hover accent are appropriate everywhere BlogCard is used, so this is fine.

**Update `src/components/sections/TransitionalCTAsSection.astro`:**

3. **Section heading** — Before the grid div, add a heading: `<h2 class="font-serif text-foreground mb-8" style="font-size: clamp(1.25rem, 2.5vw, 1.75rem);">Not ready to book? Start here.</h2>` wrapped in a SectionAnimator.

4. **Third card** — Add the third blog post ("Get Out of the AI Cage" or similar). Check `src/content/blog/get-out-of-the-ai-cage.md` frontmatter for title and excerpt. Add a third BlogCard:
   ```
   <BlogCard
     title="Get Out of the AI Cage"
     date=""
     excerpt="..."
     href="/blog/get-out-of-the-ai-cage"
   />
   ```
   Use the actual frontmatter excerpt from the blog post file.

5. **Three-column grid** — Change `grid-cols-1 md:grid-cols-2` to `grid-cols-1 md:grid-cols-3` so all three cards fill the row on desktop.

6. **Stagger animation** — The existing SectionAnimator wraps the whole grid, which gives a single fadeUp. For simple stagger without a new island, wrap each BlogCard in its own SectionAnimator with delays: `delay={0}`, `delay={0.12}`, `delay={0.24}`. Remove the outer SectionAnimator wrapping the grid div.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>Transitional section shows "Not ready to book? Start here." heading in serif, three blog cards in 3-column grid, cards have serif titles and hover accent border (border-primary), cards stagger-reveal with 0/0.12/0.24s delays</done>
</task>

</tasks>

<verification>
- `npx astro build` completes successfully
- Stakes CTA at `/#section-cta` shows glow, corner marks, accent em, pill button
- Transitional CTAs at `/#section-transitional` shows heading + 3 cards in row
- BlogCard hover shows primary border color
</verification>

<success_criteria>
- Stakes CTA has visible radial purple glow and corner marks
- "84%" and "0.04%" are highlighted in accent purple
- CTA button is pill-shaped (rounded-full)
- Transitional section heading "Not ready to book? Start here." in serif
- Three cards fill the row on desktop (3-column grid)
- Blog card titles render in serif font
- Blog card hover shows primary accent border
- Cards stagger-reveal on viewport entry
</success_criteria>

<output>
After completion, create `.planning/phases/13-social-proof-authority-stakes-cta-transitional-ctas/13-02-SUMMARY.md`
</output>
