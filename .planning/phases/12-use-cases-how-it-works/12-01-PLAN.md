---
phase: 12-use-cases-how-it-works
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/islands/UseCaseCardsIsland.tsx
  - src/components/sections/UseCasesSection.astro
autonomous: true
requirements: [RDES-25, RDES-26, RDES-27, RDES-28]

must_haves:
  truths:
    - "Use cases section displays three use cases with sequential card animation"
    - "Cards show mono uppercase labels, serif titles, typewriter descriptions, and CTA pulse"
    - "Section uses cream background with gradient border separators"
  artifacts:
    - path: "src/components/islands/UseCaseCardsIsland.tsx"
      provides: "Use case card animations (sequential card pattern)"
    - path: "src/components/sections/UseCasesSection.astro"
      provides: "Use cases section wrapper with cream bg"
  key_links:
    - from: "src/components/sections/UseCasesSection.astro"
      to: "UseCaseCardsIsland.tsx"
      via: "React island client:visible"
      pattern: "UseCaseCardsIsland.*client:visible"
---

<objective>
Verify that the use cases section (RDES-25 through RDES-28) is already implemented and working.

Purpose: The use cases section was built in a prior session using the card-based layout pattern (same as OfferingCardsIsland) rather than the alternating two-column layout originally spec'd. This was a deliberate user decision. This plan validates the existing implementation satisfies the requirements.

Output: Confirmation that UseCaseCardsIsland.tsx and UseCasesSection.astro are complete and functional.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/islands/UseCaseCardsIsland.tsx
@src/components/sections/UseCasesSection.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix card animation bug and verify use cases section</name>
  <files>src/components/islands/UseCaseCardsIsland.tsx, src/components/islands/OfferingCardsIsland.tsx</files>
  <action>
MEMORY.md documents a bug: "Card animation broken. Both OfferingCardsIsland.tsx and UseCaseCardsIsland.tsx stop after first card title prints. Animation sequence halts -- doesn't advance to description/CTA or next cards. Likely caused by timing constant changes (LETTER_SPEED 25->18, TYPE_SPEED 15->10, CARD_GAP 200->5)."

Debug the advancePhase/onComplete chain in both island components. The likely fix is reverting timing constants to values that allow the animation state machine to complete each phase:
- LETTER_SPEED: try 20-25ms (was 25, broken at 18)
- TYPE_SPEED: try 12-15ms (was 15, broken at 10)
- CARD_GAP: try 100-200ms (was 200, broken at 5)

Test that the full animation sequence completes: label/icon -> title stagger -> typewriter desc -> CTA pulse, then advances to next card. All 3 cards must complete.

Also verify:
- Cards have mono uppercase labels (RDES-26)
- Cream background on section (var(--secondary))
- Sequential animation pattern works end-to-end (RDES-28)

Note: RDES-25 spec'd alternating two-column layout but user chose card layout instead. RDES-27 spec'd image placeholders with corner marks -- replaced by card design. Both are satisfied by user's deliberate design choice.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>Card animation completes full sequence for all 3 cards in both UseCaseCardsIsland and OfferingCardsIsland. Build succeeds. RDES-25, 26, 27, 28 satisfied.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds without errors
- UseCaseCardsIsland.tsx renders 3 use case cards with sequential animation
- OfferingCardsIsland.tsx animation also fixed (same bug)
</verification>

<success_criteria>
Both card island components animate correctly through all phases. Build passes. Use cases section visually complete.
</success_criteria>

<output>
After completion, create `.planning/phases/12-use-cases-how-it-works/12-01-SUMMARY.md`
</output>
