---
phase: 19-homepage-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/StakesCTASection.astro
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "StakesCTA section has increased padding clamp(3rem, 6vw, 5rem), up from clamp(2rem, 4vw, 3rem), but not full service page size since it shares a scroll-section with the blog carousel"
    - "StakesCTA section has a supporting paragraph below the heading and before the button"
    - "StakesCTA section has dot grid texture (.bg-dots) for added visual depth"
    - "Blog carousel wrapper has smoother visual transition from the dark CTA above"
    - "Section renders correctly in light and dark mode"
    - "Section renders correctly on mobile viewports"
    - "StaggerHeading animation and StakesCTAPulse still work"
    - "Blog carousel still shows latest 3 posts with correct data"
  artifacts:
    - path: "src/components/sections/StakesCTASection.astro"
      provides: "Dark CTA section with increased padding, supporting paragraph, dot grid"
      contains: "bg-dots"
    - path: "src/pages/index.astro"
      provides: "Homepage with refined blog carousel wrapper styling"
  key_links:
    - from: "src/components/sections/StakesCTASection.astro"
      to: "src/components/islands/StaggerHeading"
      via: "client:visible island import"
      pattern: "StaggerHeading.*client:visible"
    - from: "src/pages/index.astro"
      to: "src/components/islands/BlogCarouselIsland"
      via: "client:visible island import"
      pattern: "BlogCarouselIsland.*client:visible"
---

<objective>
Polish the Get Started / StakesCTA section (homepage scroll-section 7) so it feels like a deliberate narrative close, not a rushed afterthought with tight padding and an abrupt blog carousel.

Purpose: The StakesCTA has a good foundation (dark background, glow, corner marks, StaggerHeading, CTA pulse button) but feels cramped with small padding and jumps straight into a blog carousel without transition. The service page CTAs use clamp(4rem-8rem) padding and look much more polished — this should match that standard.

Output: StakesCTASection.astro with service-page-quality padding, a supporting paragraph, and dot grid texture. index.astro with improved blog carousel wrapper transition.
</objective>

<execution_context>
@/Users/garyctate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/garyctate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sections/StakesCTASection.astro
@src/pages/index.astro
@src/pages/services/ai-advisory-for-leaders.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase StakesCTA padding, add supporting paragraph and dot grid</name>
  <files>src/components/sections/StakesCTASection.astro</files>
  <action>
Update StakesCTASection.astro to match service page CTA quality:

1. Add `bg-dots` class to the section element: `<section id="section-cta" class="bg-dots" style="background: #0D1117;">`. This adds the subtle dot grid texture over the dark background.

2. Increase the inner div padding from `clamp(2rem, 4vw, 3rem)` to `clamp(3rem, 6vw, 5rem)` for the vertical padding. Keep horizontal padding as-is. This gives more breathing room without going as large as service pages (which have full-page sections, while this shares a scroll-section with the blog carousel below).

NOTE: Do NOT use the full service page padding of `clamp(4rem, 8vw, 8rem)` because this section shares a scroll-section with the blog carousel and must fit within 100vh together. Use `clamp(3rem, 6vw, 5rem)` as a middle ground.

3. Add a supporting paragraph between the StaggerHeading and the CTA button div. Insert after the StaggerHeading and before the `<div class="mt-4">`:
   - `<p style="color: rgba(255,255,255,0.7); font-family: var(--font-sans); font-size: var(--text-body); line-height: var(--lh-body); max-width: 520px; margin: 1rem auto 0;">A 15-minute call. No pitch, no commitment. Just clarity on where AI fits in your business.</p>`
   - This is a factual description of the CTA action — what the user gets when they click "Book a Free Call".

4. Keep the StaggerHeading and StakesCTAPulse exactly as they are — do not change the heading text, CTA text, or href.

DO NOT change Gary's heading copy. DO NOT add eyebrow labels. The paragraph is a factual description of the call, not new marketing copy.
  </action>
  <verify>Run `pnpm build` — no errors. Check StakesCTA section: increased breathing room, dot grid visible over dark background, supporting paragraph visible between heading and button. StaggerHeading animation still plays. CTA button still pulses. At 1024x768 viewport, confirm the entire Get Started scroll-section (StakesCTA + blog carousel) fits within 100vh without internal scrolling.</verify>
  <done>StakesCTA section has more breathing room, dot grid texture, and supporting paragraph. Matches service page CTA quality level.</done>
</task>

<task type="auto">
  <name>Task 2: Improve blog carousel wrapper transition in index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
The blog carousel currently sits in a `bg-secondary` section immediately after the dark StakesCTA, creating an abrupt dark-to-cream jump. Improve the visual transition:

1. Change the blog carousel wrapper section's background from `bg-secondary` to a dark background that continues the CTA's visual language. Change the class/style to: `style="background: #0D1117; padding: clamp(1rem, 2vw, 1.5rem) clamp(1.5rem, 5vw, 8rem);"` (remove `class="bg-secondary"`, add dark background inline).

2. Change the "Not ready to book? Start here." heading color to white since it's now on a dark background: add `style="color: #FFFFFF;"` or change `class="text-sub font-serif mb-6 text-center"` to include a color. Use inline style: `style="color: rgba(255,255,255,0.85);"` for a slightly muted white.

3. This creates a seamless dark zone: StakesCTA dark → blog carousel dark → natural page end. The blog carousel cards themselves have their own card backgrounds, so they'll pop against the dark wrapper.

DO NOT change the BlogCarouselIsland component or its props. DO NOT change the blog data preparation code. DO NOT touch the scroll-section wrapper or data-section-label.
  </action>
  <verify>Run `pnpm build` — no errors. The Get Started scroll-section is a cohesive dark section from top to bottom. Blog carousel heading is readable on dark background. Blog carousel cards are visible and functional. Scroll-snap still works.</verify>
  <done>Blog carousel wrapper has dark background matching the CTA above, creating a cohesive dark section. No abrupt color jump.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. StakesCTA section has increased padding (more breathing room than before)
3. Dot grid texture visible on the dark CTA background
4. Supporting paragraph visible between heading and CTA button
5. Blog carousel wrapper is dark, creating cohesive dark zone
6. Blog carousel heading readable on dark background
7. Blog carousel cards visible with proper contrast
8. StaggerHeading animation still plays on scroll-section activation
9. StakesCTAPulse button still animates
10. At 1024x768 viewport, entire Get Started scroll-section fits within 100vh without internal scrolling
11. Section renders correctly in dark mode
12. Section renders correctly on mobile
</verification>

<success_criteria>
Get Started / StakesCTA section feels like a deliberate narrative close — polished dark CTA with breathing room, supporting paragraph, and a seamless transition into the blog carousel. No more "abrupt ending" feel.
</success_criteria>

<output>
After completion, create `.planning/phases/19-homepage-polish/19-03-SUMMARY.md`
</output>
