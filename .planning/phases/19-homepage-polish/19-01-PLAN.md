---
phase: 19-homepage-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HowItWorksSection.astro
  - src/components/islands/HowItWorksIsland.tsx
autonomous: true

must_haves:
  truths:
    - "How It Works section has a visible section heading 'How It Works'"
    - "How It Works section has a cream (--secondary) background, not plain white"
    - "How It Works section has gradient border separators matching OfferingSection pattern"
    - "How It Works section renders correctly in light and dark mode"
    - "How It Works section renders correctly on mobile viewports"
    - "Existing timeline animation (line draw, circle fills, content reveals) still works unchanged"
  artifacts:
    - path: "src/components/sections/HowItWorksSection.astro"
      provides: "Section wrapper with cream background, gradient borders, heading"
      contains: "How It Works"
    - path: "src/components/islands/HowItWorksIsland.tsx"
      provides: "Timeline animation component (unchanged animation, adjusted padding)"
  key_links:
    - from: "src/components/sections/HowItWorksSection.astro"
      to: "src/components/islands/HowItWorksIsland.tsx"
      via: "client:visible island import"
      pattern: "HowItWorksIsland.*client:visible"
---

<objective>
Polish the How It Works section (homepage scroll-section 4) so it has visual depth matching the surrounding sections instead of sitting on a plain white background with no heading.

Purpose: The How It Works section currently looks unfinished — no heading, no background color, no visual separation from adjacent sections. This makes it one of the "soulless middle" sections that degrades the homepage quality.

Output: HowItWorksSection.astro with cream background, gradient border separators (matching OfferingSection), and a section heading. HowItWorksIsland.tsx with padding adjustments to work within the new wrapper.
</objective>

<execution_context>
@/Users/garyctate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/garyctate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sections/HowItWorksSection.astro
@src/components/sections/OfferingSection.astro
@src/components/islands/HowItWorksIsland.tsx
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cream background, gradient borders, and heading to HowItWorksSection.astro</name>
  <files>src/components/sections/HowItWorksSection.astro</files>
  <action>
Rewrite HowItWorksSection.astro to match the OfferingSection pattern for visual consistency:

1. Add a `class="hiw-section"` to the section element with `id="section-how"`.
2. Add a `<style>` block (scoped) with:
   - `.hiw-section` gets `padding: clamp(2rem, 4vw, 3.5rem) 0; background: var(--secondary); position: relative;` (tighter than var(--section-py) to ensure the timeline content fits within 100vh)
   - `::before` and `::after` pseudo-elements for top/bottom gradient border separators — copy the exact pattern from OfferingSection (linear-gradient with transparent edges and var(--border) center).
3. Add a content wrapper div: `class="max-w-[1200px] mx-auto"` with `style="padding: 0 clamp(1.5rem, 5vw, 8rem);"`.
4. Inside the wrapper, before the island, add a centered heading block:
   - `<div class="text-center" style="margin-bottom: 2.5rem;">`
   - `<h2 class="text-section text-foreground">How It Works</h2>`
   - No subtitle paragraph (the steps speak for themselves).
5. Keep `<HowItWorksIsland client:visible />` as-is.

DO NOT add eyebrow labels (Anti-Slop Rule 01). DO NOT add gradient buttons or generic fade-up (Anti-Slop Rules 02, 03). DO NOT change Gary's copy.

NOTE: Plain cream background (no .bg-dots) is intentional for HIW. The adjacent Why Consultates section (19-02) uses .bg-dots on cream — keeping HIW without dots creates visual differentiation between the two cream sections.
  </action>
  <verify>Run `pnpm build` — no errors. Open dev server and navigate to homepage section 4 (How It Works). Confirm: cream background visible, gradient border lines at top and bottom, "How It Works" heading centered above the timeline. At 1024x768 viewport, confirm the entire How It Works scroll-section fits within 100vh without internal scrolling or content overflow.</verify>
  <done>HowItWorksSection.astro has cream background, gradient borders, and section heading. Visual pattern matches OfferingSection.</done>
</task>

<task type="auto">
  <name>Task 2: Adjust HowItWorksIsland padding to work within new section wrapper</name>
  <files>src/components/islands/HowItWorksIsland.tsx</files>
  <action>
The HowItWorksIsland currently has its own padding: `padding: 'clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem)'` on the container div. Since the section wrapper now provides section-level padding (via `var(--section-py)`) and horizontal padding (via the wrapper div), the island's padding needs adjustment:

1. Remove the vertical padding from the island's container div. The section wrapper handles vertical padding via `var(--section-py)`.
2. Remove the horizontal padding from the island's container div. The wrapper div handles horizontal padding.
3. Keep the `max-w-[1200px] mx-auto` on the island container OR remove it if it's now redundant with the wrapper div in the Astro component. Since the wrapper already has `max-w-[1200px] mx-auto`, remove it from the island to avoid double-wrapping.
4. The container div should have no padding and no max-width constraint (the Astro wrapper handles both).

DO NOT touch the animation logic, timing constants, step data, or any other behavior. Only adjust the container div's styling.
  </action>
  <verify>Run `pnpm build` — no errors. Check that the How It Works section content is properly contained within the 1200px max-width with correct horizontal padding. Timeline animation still plays on scroll-section activation. Check mobile viewport — content still centered and readable.</verify>
  <done>HowItWorksIsland container no longer has redundant padding/max-width. Section layout is controlled by the Astro wrapper. Animation unchanged.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. Homepage How It Works section (scroll-section 4) shows cream background, not white
3. Gradient border lines visible at top and bottom of the section
4. "How It Works" heading visible and centered above the timeline
5. Timeline animation (line draw, circle fills, content reveals, CTA) still works on scroll-section activation
6. Section renders correctly in dark mode (cream adapts to dark secondary)
7. Section renders correctly on mobile (single column, proper padding)
8. No GSAP scroll-snap behavior affected
9. At 1024x768 viewport, entire section fits within 100vh without overflow
</verification>

<success_criteria>
How It Works section has visual depth — cream background, gradient borders, and section heading — matching the quality of OfferingSection. Animation behavior is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/19-homepage-polish/19-01-SUMMARY.md`
</output>
