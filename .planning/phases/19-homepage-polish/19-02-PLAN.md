---
phase: 19-homepage-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/WhyConsultatesSection.astro
  - src/components/islands/AuthorityCardsIsland.tsx
autonomous: true

must_haves:
  truths:
    - "Why Consultates section has a visible section heading 'Why Consultates?'"
    - "Why Consultates section has a cream (--secondary) background with dot grid texture (.bg-dots)"
    - "Why Consultates section has gradient border separators at top and bottom"
    - "Authority cards have a subtle purple accent — stat text in var(--primary) with a faint glow or emphasis"
    - "Section renders correctly in light and dark mode"
    - "Section renders correctly on mobile viewports (cards stack to single column)"
    - "Card hover states still work (shadow elevation, border color change)"
  artifacts:
    - path: "src/components/sections/WhyConsultatesSection.astro"
      provides: "Section wrapper with cream+dots background, gradient borders, heading"
      contains: "Why Consultates"
    - path: "src/components/islands/AuthorityCardsIsland.tsx"
      provides: "Authority cards with visual refinements"
  key_links:
    - from: "src/components/sections/WhyConsultatesSection.astro"
      to: "src/components/islands/AuthorityCardsIsland.tsx"
      via: "client:visible island import"
      pattern: "AuthorityCardsIsland.*client:visible"
---

<objective>
Completely redo the Why Consultates section (homepage scroll-section 6) so it is visually distinct and engaging instead of a plain white box with three cards.

Purpose: This is the worst of the "soulless middle" sections — no heading, no background, no visual distinction. It needs to feel like a deliberate design choice, not a placeholder. Using cream background with dot grid texture (not dark) to avoid two adjacent dark sections (the next section, Get Started/StakesCTA, is already dark).

Output: WhyConsultatesSection.astro with cream+dots background, gradient borders, heading. AuthorityCardsIsland.tsx with visual refinements to the cards.
</objective>

<execution_context>
@/Users/garyctate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/garyctate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sections/WhyConsultatesSection.astro
@src/components/islands/AuthorityCardsIsland.tsx
@src/components/sections/OfferingSection.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cream+dots background, gradient borders, and heading to WhyConsultatesSection.astro</name>
  <files>src/components/sections/WhyConsultatesSection.astro</files>
  <action>
Rewrite WhyConsultatesSection.astro with proper visual treatment:

1. Change the section element to `<section id="section-why" class="why-section bg-dots">`.
2. Remove the inline `style` attribute from the section (padding will be handled by the CSS class).
3. Keep the content wrapper: `<div class="max-w-[1200px] mx-auto" style="padding: 0 clamp(1.5rem, 5vw, 8rem);">`.
4. Add a centered heading block BEFORE the island:
   - `<div class="text-center" style="margin-bottom: 2.5rem;">`
   - `<h2 class="text-section text-foreground">Why Consultates?</h2>`
   - `<p class="text-body-lg mx-auto" style="max-width: 580px; color: var(--secondary-foreground);">Gary Tate brings a rare combination — deep technical experience, real sales leadership, and daily AI practice.</p>`
   - Close the div.
5. Keep `<AuthorityCardsIsland client:visible />` as-is.
6. Add a `<style>` block with:
   - `.why-section` gets `padding: var(--section-py) 0; background: var(--secondary); position: relative;`
   - `::before` and `::after` pseudo-elements for gradient border separators (same pattern as OfferingSection).

The subtitle text in step 4 is a summary of the card content — it frames the three columns. DO NOT add eyebrow labels. DO NOT add gradient buttons or generic fade-up.

IMPORTANT: The subtitle copy "Gary Tate brings a rare combination — deep technical experience, real sales leadership, and daily AI practice." is NOT from the PRD. It is a factual summary of the card content below it — but it REQUIRES GARY'S APPROVAL before execution since it is new copy not sourced from the PRD. If Gary has not approved it, omit the `<p>` subtitle entirely and keep just the `<h2>` heading.
  </action>
  <verify>Run `pnpm build` — no errors. Check that the Why Consultates section has cream background with visible dot grid pattern, gradient borders, heading, and subtitle.</verify>
  <done>WhyConsultatesSection.astro has cream+dots background, gradient borders, heading with subtitle. Section is visually distinct from surrounding sections.</done>
</task>

<task type="auto">
  <name>Task 2: Refine AuthorityCardsIsland card visuals</name>
  <files>src/components/islands/AuthorityCardsIsland.tsx</files>
  <action>
Add subtle visual refinements to make the authority cards feel more polished within their new section:

1. Add a thin top border accent to each card — a 3px solid `var(--primary)` border-top on the card. Add `borderTop: '3px solid var(--primary)'` to the `cardStyle` object. This gives each card a purple accent line that ties into the brand.

2. Make the stat text more prominent — the stat already uses `text-primary` class and `font-serif`. Increase the `fontWeight` from 700 to 800 (if the font supports it) or keep at 700. Add `letterSpacing: '-0.02em'` to the stat `<p>` style to make the large text feel tighter and more impactful.

3. Ensure the card background works on cream — the cards use `var(--card)` which should be white/near-white on cream. This should already work, but verify the cards have visible contrast against the cream section background.

DO NOT change the card content/copy. DO NOT change the animation behavior (Framer Motion whileInView, delay, etc.). DO NOT change the grid layout (3-col on desktop, 1-col on mobile).
  </action>
  <verify>Run `pnpm build` — no errors. Cards have purple top border accent. Stat text has tight letter-spacing. Cards are visually distinct from the cream background. Hover states still work. Cards render correctly in dark mode.</verify>
  <done>Authority cards have purple top border accent and refined stat typography. Cards visually pop against the cream+dots background.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. Why Consultates section has cream background with dot grid texture
3. Gradient border lines visible at top and bottom
4. "Why Consultates?" heading and subtitle centered above cards
5. Three authority cards have purple top border accent
6. Card hover states work (shadow + border color change)
7. Section renders correctly in dark mode
8. Section renders correctly on mobile (cards stack, heading readable)
9. No adjacent-dark-section issue (cream here, dark in next section)
</verification>

<success_criteria>
Why Consultates section is visually distinct and engaging — cream+dots background, heading with subtitle, cards with purple accents. No longer a "soulless" plain white box.
</success_criteria>

<output>
After completion, create `.planning/phases/19-homepage-polish/19-02-SUMMARY.md`
</output>
