---
phase: 11-negative-stakes-offering
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/ServiceCard.astro
  - src/components/sections/OfferingSection.astro
autonomous: false
requirements:
  - RDES-21
  - RDES-22
  - RDES-23
  - RDES-24

must_haves:
  truths:
    - "Offering cards have 16px radius, gradient icon containers, serif titles, and inset accent hover"
    - "Cards stagger-reveal on scroll entrance with 0s, 0.12s, 0.24s delays"
    - "Card gap is 1.25rem between cards"
    - "Visual fidelity confirmed via Playwright screenshots at 3 viewports"
  artifacts:
    - path: "src/components/ServiceCard.astro"
      provides: "Design system card with gradient icon container, serif title, inset accent hover"
      min_lines: 20
    - path: "src/components/sections/OfferingSection.astro"
      provides: "Offering section with staggered card reveal and 1.25rem gap"
      min_lines: 30
  key_links:
    - from: "src/components/sections/OfferingSection.astro"
      to: "src/components/ServiceCard.astro"
      via: "Astro component import"
      pattern: "import ServiceCard"
    - from: "src/components/sections/OfferingSection.astro"
      to: "src/components/islands/SectionAnimator.tsx"
      via: "React island for stagger animation"
      pattern: "SectionAnimator.*client:visible"
---

<objective>
Apply design system treatment to the Offering section cards and add stagger-reveal animation. Then take Playwright screenshots of both redesigned sections (Negative Stakes + Offering) for visual verification.

Purpose: The offering section is the "Solution" beat answering the problem. Cards appearing one by one continues the sequential narrative rhythm established by the stroke-draw stats.

Output: Redesigned ServiceCard.astro and OfferingSection.astro matching design system spec. Playwright screenshots for human review.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-negative-stakes-offering/11-CONTEXT.md
@.planning/phases/11-negative-stakes-offering/11-01-SUMMARY.md

@src/components/ServiceCard.astro
@src/components/sections/OfferingSection.astro
@src/components/islands/SectionAnimator.tsx
@src/styles/global.css

<interfaces>
<!-- Current ServiceCard interface -->
From src/components/ServiceCard.astro:
```typescript
interface Props {
  icon?: string;
  title: string;
  description: string;
  href: string;
}
```

<!-- SectionAnimator for stagger delays -->
From src/components/islands/SectionAnimator.tsx:
```typescript
interface SectionAnimatorProps {
  children: ReactNode;
  variant?: 'fadeUp' | 'stagger';
  delay?: number;
  className?: string;
  as?: 'div' | 'p' | 'h2';
}
```

<!-- Design system tokens -->
From src/styles/global.css:
```css
--card-padding: clamp(1.75rem, 3vw, 2.25rem);
--card-radius: 16px;
--stagger: 0.12s;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply design system treatment to ServiceCard and OfferingSection</name>
  <files>src/components/ServiceCard.astro, src/components/sections/OfferingSection.astro</files>
  <action>
**ServiceCard.astro changes:**

1. **Gradient icon container:** Wrap the `<slot name="icon" />` in a container div:
   - 48px width/height, 12px border-radius
   - Background: `linear-gradient(135deg, var(--primary) 0%, var(--primary-lifted, #8B6CC7) 100%)` with ~15% opacity
   - Center the icon inside with flexbox
   - Icon color: `var(--primary)`

2. **Card radius:** Change `rounded-xl` to use `var(--card-radius)` (16px) -- can use `rounded-[16px]` or inline style

3. **Serif titles:** Change h3 from `text-sub text-card-foreground` to include `font-serif` (IBM Plex Serif)

4. **Inset accent hover effect:** Replace the current `hover:-translate-y-1 hover:shadow-[...]` with:
   - `hover:border-primary/40` (accent border on hover)
   - `hover:shadow-[inset_0_0_0_1px_var(--primary)]` (inset accent shadow)
   - Keep the transition-all duration-200
   - Remove the translate-y lift (design system uses inset glow, not lift)

5. **Card padding:** Use `var(--card-padding)` instead of hardcoded `p-8`

**OfferingSection.astro changes:**

1. **Card gap:** Change `gap-6` to `gap-[1.25rem]` (1.25rem per spec)

2. **Individual card stagger:** Instead of wrapping all cards in one SectionAnimator, wrap EACH ServiceCard in its own SectionAnimator with staggered delays:
   - Card 1: `delay={0}` (0s)
   - Card 2: `delay={0.12}` (0.12s)
   - Card 3: `delay={0.24}` (0.24s)

3. **Section heading:** Keep existing heading and paragraph, but wrap in SectionAnimator with `delay={0}` as before

4. **Keep all existing card content/copy/icons unchanged**
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -20 && grep -c "font-serif" src/components/ServiceCard.astro && grep -c "1.25rem" src/components/sections/OfferingSection.astro</automated>
  </verify>
  <done>ServiceCard has gradient icon container (48px, 12px radius, gradient bg), serif title, 16px card radius, inset accent hover, design system padding. OfferingSection has 1.25rem gap, individual card stagger at 0/0.12/0.24s delays. All existing content preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Negative Stakes and Offering sections</name>
  <action>
Human verifies both redesigned sections visually.

What was built: Negative Stakes section with 3-col text-stroke stats, stroke-draw animation, section title, pull quote, and radial glow. Offering section with design system cards (gradient icons, serif titles, inset hover, stagger reveal).

How to verify:
1. Run `cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro dev` (if not already running)
2. Visit http://localhost:4321
3. Scroll to the Negative Stakes section (section 2):
   - Verify serif title appears: "The ground is moving faster than most people realise"
   - Verify 3 stats in columns: 84%, 5% to 40%, 12-18 months
   - Verify stat numbers have text-stroke outline treatment (transparent fill, purple stroke)
   - Verify stroke-draw animation traces the number outlines sequentially
   - Verify pull quote from Kevin Roose appears below stats
   - Verify subtle radial glow behind section
4. Scroll to the Offering section (section 3):
   - Verify 3 cards with gradient icon backgrounds (purple tinted)
   - Verify card titles are in serif font
   - Hover each card -- verify inset accent border effect (not lift)
   - Verify cards appeared with stagger timing
5. Resize to mobile width -- verify stats and cards stack to single column
6. Take Playwright screenshots if desired: desktop (1920x1080), tablet (768x1024), mobile (390x844)
  </action>
  <verify>Human visual confirmation</verify>
  <done>Human approves visual fidelity of both sections or provides specific feedback for fixes</done>
</task>

</tasks>

<verification>
1. `npx astro check` passes with no errors
2. `npx astro build` completes successfully
3. ServiceCard renders with gradient icon container, serif title, inset hover, 16px radius
4. OfferingSection uses 1.25rem gap, individual card stagger at correct delays
5. Both sections render correctly at desktop, tablet, and mobile widths
6. Human confirms visual fidelity
</verification>

<success_criteria>
- Offering cards match design system: 16px radius, gradient icon containers, serif titles, inset accent hover
- Cards stagger-reveal at 0s, 0.12s, 0.24s on scroll entrance
- Card gap is 1.25rem
- Human approves visual fidelity of both Negative Stakes and Offering sections
</success_criteria>

<output>
After completion, create `.planning/phases/11-negative-stakes-offering/11-02-SUMMARY.md`
</output>
