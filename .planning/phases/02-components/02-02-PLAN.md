---
phase: 02-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ServiceCard.astro
  - src/components/TestimonialBlock.astro
  - src/components/StatBlock.astro
  - src/components/StepCard.astro
  - src/components/BlogCard.astro
  - src/components/FormInput.astro
  - src/components/ScrollIndicator.astro
autonomous: true
requirements: [COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-09]

must_haves:
  truths:
    - "ServiceCard shows icon, title, description, and learn-more link with hover lift"
    - "TestimonialBlock shows decorative quote mark, quote text, name, and title"
    - "StatBlock shows number in mono font, label, and source citation"
    - "StepCard shows numbered circle with heading and description"
    - "BlogCard shows title, date, excerpt, and read link with hover lift"
    - "FormInput renders label, input/textarea, error state, and focus ring"
    - "ScrollIndicator shows mouse outline with inner dot and bob animation"
  artifacts:
    - path: "src/components/ServiceCard.astro"
      provides: "Card with icon, title, desc, link, hover lift"
    - path: "src/components/TestimonialBlock.astro"
      provides: "Quote block with decorative mark and attribution"
    - path: "src/components/StatBlock.astro"
      provides: "Stat display with number, label, source"
    - path: "src/components/StepCard.astro"
      provides: "Numbered step with heading and description"
    - path: "src/components/BlogCard.astro"
      provides: "Blog post card with hover lift"
    - path: "src/components/FormInput.astro"
      provides: "Form field with label, error, focus states"
    - path: "src/components/ScrollIndicator.astro"
      provides: "Scroll hint animation"
  key_links:
    - from: "all components"
      to: "src/styles/global.css"
      via: "design tokens"
      pattern: "var\\(--"
---

<objective>
Build all 7 shared display/form components as Astro components using design tokens from global.css. These are pure presentation components with no interactivity dependencies.

Purpose: Provides the full component toolkit that homepage sections and inner pages compose from. Independent of Plan 01 (no theme toggle or icon wrapper needed — these use Tailwind classes and CSS vars directly).
Output: Seven Astro components in src/components/.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-components/02-CONTEXT.md

<interfaces>
<!-- Design tokens from global.css used by all components -->

Type scale classes: text-hero, text-section, text-sub, text-body-lg, text-body, text-caption, text-stats, text-cite
Font families: font-sans, font-serif, font-mono
Colors: bg-primary, text-primary, bg-card, text-card-foreground, text-muted-foreground, bg-muted, border-border, outline-ring, text-destructive
Keyframes: scroll-bob (2s infinite ease-in-out, translateY 0->12px->0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create card-style components (ServiceCard, BlogCard, StepCard, StatBlock)</name>
  <files>src/components/ServiceCard.astro, src/components/BlogCard.astro, src/components/StepCard.astro, src/components/StatBlock.astro</files>
  <action>
All Astro components. No React needed — these are static presentation.

**ServiceCard.astro** (COMP-02):
- Props: `icon` (string — Phosphor icon name for reference, rendered as slot), `title` (string), `description` (string), `href` (string)
- Use a `<slot name="icon" />` for the icon so the parent can pass any Phosphor SVG or React island
- Container: `<a>` tag wrapping card. Classes: `block bg-card border border-border rounded-xl p-8 transition-all duration-200 hover:-translate-y-1 hover:shadow-[0_10px_25px_rgba(0,0,0,0.1)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring`
- Icon area: `<div class="mb-4 text-primary">` containing the icon slot (32px)
- Title: `<h3 class="text-sub text-card-foreground mb-2">`
- Description: `<p class="text-body text-muted-foreground mb-4">`
- Link text: `<span class="text-body font-medium text-primary">Learn more &rarr;</span>`

**BlogCard.astro** (COMP-06):
- Props: `title` (string), `date` (string), `excerpt` (string), `href` (string)
- Container: `<a>` tag. Classes: `block bg-card border border-border rounded-xl p-6 transition-all duration-200 hover:-translate-y-0.5 hover:shadow-[0_4px_12px_rgba(0,0,0,0.08)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring`
- Title: `<h3 class="text-sub text-card-foreground mb-1">`
- Date: `<p class="text-cite text-muted-foreground mb-3">`
- Excerpt: `<p class="text-body text-muted-foreground mb-4 line-clamp-3">`
- Link: `<span class="text-body font-medium text-primary">Read &rarr;</span>`

**StepCard.astro** (COMP-05):
- Props: `number` (number), `heading` (string), `description` (string)
- Numbered circle: `<div class="w-12 h-12 rounded-full border-2 border-primary flex items-center justify-center"><span class="font-mono font-bold text-lg text-primary">{number}</span></div>`
- Heading: `<h3 class="text-sub mt-4">`
- Description: `<p class="text-body text-muted-foreground mt-2">`

**StatBlock.astro** (COMP-04):
- Props: `number` (string — e.g. "84%"), `label` (string), `source?` (string)
- Number: `<p class="text-stats text-primary">`
- Label: `<p class="text-body text-foreground mt-2">`
- Source: `<p class="text-cite text-muted-foreground mt-1">` (only render if source provided)
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Four card/stat components render with correct typography, colors, hover states, and focus rings per PRD specs.</done>
</task>

<task type="auto">
  <name>Task 2: Create content and form components (TestimonialBlock, FormInput, ScrollIndicator)</name>
  <files>src/components/TestimonialBlock.astro, src/components/FormInput.astro, src/components/ScrollIndicator.astro</files>
  <action>
**TestimonialBlock.astro** (COMP-03):
- Props: `quote` (string), `name` (string), `title` (string)
- Container: `<blockquote class="relative border-l-[3px] border-primary pl-6">`
- Decorative quote mark: `<span class="absolute -top-4 -left-2 font-serif font-bold text-[6rem] leading-none text-primary opacity-20 select-none pointer-events-none" aria-hidden="true">&ldquo;</span>`
- Quote text: `<p class="text-body-lg font-serif italic text-foreground">`
- Name: `<p class="text-body font-semibold text-foreground mt-4">`
- Title: `<p class="text-caption text-muted-foreground">`

**FormInput.astro** (COMP-07):
- Props: `label` (string), `name` (string), `type?` (string, default `'text'`), `required?` (boolean), `error?` (string), `textarea?` (boolean), `placeholder?` (string), `disabled?` (boolean)
- Wrapper: `<div class="flex flex-col gap-1">`
- Label: `<label for={name} class="text-caption font-medium text-foreground">`
- Input (when not textarea): `<input id={name} name={name} type={type} placeholder={placeholder} required={required} disabled={disabled} class="bg-card border rounded-lg px-3 py-2.5 font-sans text-base text-foreground placeholder:text-muted-foreground focus:outline-2 focus:outline-offset-0 focus:outline-ring disabled:opacity-50 disabled:cursor-not-allowed {error ? 'border-destructive' : 'border-border'}" />`
- Textarea (when textarea=true): same classes but `<textarea rows="5">`
- Error message: `{error && <p class="text-cite text-destructive">{error}</p>}`

**ScrollIndicator.astro** (COMP-09):
- No props needed
- Container: `<div class="absolute bottom-8 left-1/2 -translate-x-1/2" style="animation: scroll-bob 2s infinite ease-in-out;">`
- Mouse outline: `<div class="w-[30px] h-[50px] border-2 border-foreground rounded-[15px] opacity-60 flex justify-center">`
- Inner dot: `<div class="w-1.5 h-1.5 rounded-full bg-foreground mt-1.5"></div>`
- Add `aria-hidden="true"` to the container (decorative element)
- Note: fade-on-scroll behavior will be wired in Phase 4 (GSAP)
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>TestimonialBlock shows decorative quote, attribution. FormInput handles text/textarea/error/disabled. ScrollIndicator bobs with animation.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes with zero errors
- All 7 component files exist in src/components/
- Each component uses design tokens (no hardcoded colors)
- Hover states use transition classes
- Focus states use focus-visible outline
</verification>

<success_criteria>
Seven shared display components built and building without errors. Each uses design tokens from global.css. Card components have hover lift effects. FormInput handles error and disabled states. ScrollIndicator animates with scroll-bob keyframe.
</success_criteria>

<output>
After completion, create `.planning/phases/02-components/02-02-SUMMARY.md`
</output>
