---
phase: 03-homepage
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/pages/index.astro
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06, HOME-07, HOME-08, HOME-09, HOME-10]

must_haves:
  truths:
    - "Homepage renders all 10 sections in correct StoryBrand narrative order"
    - "Homepage has no placeholder text or missing sections"
    - "Book a Free Call buttons link to TidyCal URL and open in new tab"
    - "Service cards link to correct service page URLs"
    - "Blog cards link to correct blog post URLs"
    - "Page builds without errors via pnpm build"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete homepage composing all sections"
      min_lines: 20
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/sections/"
      via: "imports all 10 section components"
      pattern: "import.*Section"
    - from: "src/pages/index.astro"
      to: "src/layouts/Base.astro"
      via: "wraps content in Base layout"
      pattern: "import Base"
---

<objective>
Wire all 10 homepage sections into index.astro and verify the complete homepage builds and renders.

Purpose: Assembles the full StoryBrand narrative page. This is the moment all section components come together into the visitor-facing homepage.

Output: Updated `src/pages/index.astro` with all sections composed in order.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-CONTEXT.md
@.planning/phases/03-homepage/03-01-SUMMARY.md
@.planning/phases/03-homepage/03-02-SUMMARY.md

<interfaces>
<!-- Section components created by Plans 01 and 02 -->
<!-- All are pure Astro components with no props — content is baked in -->

src/components/sections/HeroSection.astro      (id="section-hero")
src/components/sections/TrustBar.astro          (id="section-trust")
src/components/sections/NegativeStakes.astro    (id="section-stakes")
src/components/sections/OfferingSection.astro   (id="section-offering")
src/components/sections/UseCasesSection.astro   (id="section-cases")
src/components/sections/HowItWorksSection.astro (id="section-how")
src/components/sections/SocialProofSection.astro(id="section-proof")
src/components/sections/WhyConsultatesSection.astro (id="section-why")
src/components/sections/StakesCTASection.astro  (id="section-cta")
src/components/sections/TransitionalCTAsSection.astro (id="section-transitional")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire index.astro with all homepage sections</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the current design system test content in `src/pages/index.astro` with the complete homepage.

1. Import Base layout and all 10 section components
2. Wrap in `<Base title="Consultates — AI Consulting for Business Leaders" description="Clarity on AI for leaders, teams, and companies. One-to-one coaching, hands-on training, and fractional executive support from someone who builds with AI every day.">` (title and description from PRD §1.1 route table)
3. Compose sections in StoryBrand narrative order (Nav and Footer come from Base.astro automatically):
   ```
   <HeroSection />
   <TrustBar />
   <NegativeStakes />
   <OfferingSection />
   <UseCasesSection />
   <HowItWorksSection />
   <SocialProofSection />
   <WhyConsultatesSection />
   <StakesCTASection />
   <TransitionalCTAsSection />
   ```
4. No wrapper div needed — sections handle their own backgrounds and widths
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -c "import.*from.*sections" src/pages/index.astro | grep -qE "^10$" && echo "10 section imports" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>index.astro imports and renders all 10 sections in order. `pnpm build` completes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and fix any issues</name>
  <files>src/pages/index.astro</files>
  <action>
1. Run `pnpm build` and verify it completes with no errors
2. If there are import errors, missing files, or type issues — fix them
3. Run `pnpm dev` briefly to confirm the dev server starts
4. Check that all section `id` attributes are present in the built HTML:
   - `grep -o 'id="section-[^"]*"' dist/index.html | sort` should show all 10 section IDs
5. Verify no placeholder text remains — search built output for common placeholder patterns like "TODO", "Lorem", "placeholder"
6. Verify all TidyCal links are present: `grep -c "tidycal.com" dist/index.html` should show 3+ (Hero CTA, How It Works CTA, Stakes CTA)
7. Verify service page links: `grep -c "/services/" dist/index.html` should show 3
8. Verify blog links: `grep -c "/blog/" dist/index.html` should show 2
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build && grep -o 'id="section-[^"]*"' dist/index.html | wc -l | grep -qE "^10$" && echo "All 10 sections present" && grep -c "tidycal.com" dist/index.html && grep -c "/services/" dist/index.html && grep -c "/blog/" dist/index.html</automated>
  </verify>
  <done>Homepage builds cleanly. All 10 section IDs present in output. TidyCal links appear 3+ times. 3 service page links and 2 blog links present. No placeholder text.</done>
</task>

</tasks>

<verification>
`pnpm build` passes. Built HTML contains all 10 section IDs, correct TidyCal URLs, service page links, and blog post links. No placeholder or TODO text in output.
</verification>

<success_criteria>
- `pnpm build` completes with zero errors
- Built index.html contains all 10 section IDs (section-hero through section-transitional)
- 3+ TidyCal links present (Hero, How It Works, Stakes CTA)
- 3 service page links present (/services/ai-coaching-for-leaders, /services/ai-training-for-teams, /services/fractional-exec-support)
- 2 blog links present (/blog/youre-not-the-only-one-stuck, /blog/how-safe-is-ai-really)
- Homepage reads as coherent narrative from hero to final CTA
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-03-SUMMARY.md`
</output>
