---
phase: 03-homepage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HowItWorksSection.astro
  - src/components/sections/SocialProofSection.astro
  - src/components/sections/WhyConsultatesSection.astro
  - src/components/sections/StakesCTASection.astro
  - src/components/sections/TransitionalCTAsSection.astro
autonomous: true
requirements: [HOME-06, HOME-07, HOME-08, HOME-09, HOME-10]

must_haves:
  truths:
    - "How It Works section shows 3 numbered steps and a Book a Free Call CTA"
    - "Social Proof section shows Frank Wiener quote on secondary background with decorative quote mark"
    - "Why Consultates section shows 3 authority columns"
    - "Stakes CTA section is always-dark with white text and purple CTA button"
    - "Transitional CTAs section shows 2 blog cards"
  artifacts:
    - path: "src/components/sections/HowItWorksSection.astro"
      provides: "3-step process with CTA"
      contains: "section-how"
    - path: "src/components/sections/SocialProofSection.astro"
      provides: "Full testimonial on secondary bg"
      contains: "section-proof"
    - path: "src/components/sections/WhyConsultatesSection.astro"
      provides: "3 authority columns"
      contains: "section-why"
    - path: "src/components/sections/StakesCTASection.astro"
      provides: "Always-dark urgency CTA"
      contains: "section-cta"
    - path: "src/components/sections/TransitionalCTAsSection.astro"
      provides: "2 blog card links"
      contains: "section-transitional"
  key_links:
    - from: "src/components/sections/HowItWorksSection.astro"
      to: "src/components/StepCard.astro"
      via: "3 step card instances"
      pattern: "import StepCard"
    - from: "src/components/sections/HowItWorksSection.astro"
      to: "src/components/Button.astro"
      via: "CTA button"
      pattern: "import Button"
    - from: "src/components/sections/TransitionalCTAsSection.astro"
      to: "src/components/BlogCard.astro"
      via: "2 blog card instances"
      pattern: "import BlogCard"
    - from: "src/components/sections/StakesCTASection.astro"
      to: "src/components/Button.astro"
      via: "CTA button with forced light-mode primary"
      pattern: "import Button"
---

<objective>
Build the bottom 5 homepage sections (How It Works through Transitional CTAs).

Purpose: Completes the StoryBrand narrative arc — process clarity, social proof, authority, urgency, and a catch-net for visitors not ready to book yet.

Output: 5 Astro section components in `src/components/sections/`.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-CONTEXT.md
@PRD.md (§4 Sections 7-11 for exact copy, CSS values, and layout specs)

<interfaces>
<!-- Existing components to import and use -->

From src/components/StepCard.astro:
```astro
interface Props {
  number: number;
  heading: string;
  description: string;
}
```

From src/components/Button.astro:
```astro
interface Props {
  variant?: 'primary' | 'secondary' | 'ghost' | 'cta';
  size?: 'default' | 'large';
  href?: string;
  class?: string;
}
```

From src/components/BlogCard.astro:
```astro
interface Props {
  title: string;
  date: string;
  excerpt: string;
  href: string;
}
```

From src/config.ts (created in Plan 01, but constants are simple strings — inline if needed):
```ts
export const TIDYCAL_URL = 'https://tidycal.com/garyctate/15-minute-meeting';
export const CTA_TEXT = 'Book a Free Call';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create How It Works and Social Proof sections</name>
  <files>src/components/sections/HowItWorksSection.astro, src/components/sections/SocialProofSection.astro</files>
  <action>
1. Create `src/components/sections/HowItWorksSection.astro`:
   - `id="section-how"`
   - Container: `max-w-[1200px] mx-auto` with padding `clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem)` via inline style
   - Steps grid: `grid grid-cols-3 gap-8 mb-8`, mobile (`max-md:`): `grid-cols-1`
   - Import StepCard from `../StepCard.astro`
   - 3 StepCards with PRD copy:
     - Step 1: number=1, heading="We talk", description="Tell me where you are and what you're trying to figure out. No preparation needed."
     - Step 2: number=2, heading="You get a plan", description="Specific to your business, your team, and your goals. Not a template."
     - Step 3: number=3, heading="We do the work together", description="Coaching, training, or hands-on leadership. One accountable team, start to finish."
   - CTA below grid, centered (`text-center`): Import Button, `variant="cta"`, href to TidyCal URL (inline the URL string: `https://tidycal.com/garyctate/15-minute-meeting`), `target="_blank" rel="noopener noreferrer"`. Text: `Book a Free Call`
   - Note: If src/config.ts doesn't exist yet (Plan 01 may not have run), inline the URL directly. Both plans are Wave 1 and may run in parallel.

2. Create `src/components/sections/SocialProofSection.astro`:
   - `id="section-proof"`
   - Full-bleed secondary background: outer wrapper with `bg-secondary`
   - Inner container: `max-w-[800px] mx-auto relative` with padding `clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem)` via inline style
   - Decorative quote mark: `<span>` with classes `absolute font-serif font-bold text-[6rem] leading-none text-primary opacity-20 select-none pointer-events-none` and inline style `top: clamp(2rem, 4vw, 4rem); left: clamp(1.5rem, 5vw, 8rem);`. Content: `"` (use `&ldquo;` entity). `aria-hidden="true"`
   - Quote: `<p>` with `text-body-lg font-serif italic text-foreground`. Full Frank Wiener quote verbatim from PRD §4 Section 8 (the long quote starting "I originally hired him into Cyan...")
   - Name: `<p class="text-body font-semibold text-foreground mt-6">Frank Wiener</p>`
   - Title: `<p class="text-caption text-muted-foreground">CMO and Director of Product Marketing (retired)</p>`
   - Do NOT use TestimonialBlock — PRD specifies different visual treatment (no border-left, decorative quote positioned differently, secondary bg)
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -l "section-how" src/components/sections/HowItWorksSection.astro && grep -l "section-proof" src/components/sections/SocialProofSection.astro && grep -c "StepCard" src/components/sections/HowItWorksSection.astro</automated>
  </verify>
  <done>How It Works shows 3 numbered steps with CTA below. Social Proof shows Frank Wiener quote with decorative mark on secondary background.</done>
</task>

<task type="auto">
  <name>Task 2: Create Why Consultates, Stakes CTA, and Transitional CTAs sections</name>
  <files>src/components/sections/WhyConsultatesSection.astro, src/components/sections/StakesCTASection.astro, src/components/sections/TransitionalCTAsSection.astro</files>
  <action>
1. Create `src/components/sections/WhyConsultatesSection.astro`:
   - `id="section-why"`
   - Container: `max-w-[1200px] mx-auto` with section padding via inline style
   - Columns grid: `grid grid-cols-3 gap-8`, mobile (`max-md:`): `grid-cols-1`
   - 3 columns, each a `<div>`:
     - Column 1: heading (`text-sub text-foreground`) "20 years building technology", paragraph (`text-body text-muted-foreground mt-3`) with exact PRD copy about RAF, telecoms, Juniper, Cyan
     - Column 2: heading "15 years selling it", paragraph with PRD copy about SE to VP Sales to CRO
     - Column 3: heading "Now builds with AI agents daily", paragraph with PRD copy about Claude Code, Codex, FlexOS
   - All copy verbatim from PRD §4 Section 9

2. Create `src/components/sections/StakesCTASection.astro`:
   - `id="section-cta"`
   - ALWAYS DARK regardless of theme. Outer wrapper: inline style `background: #0D1117;` (not a theme token)
   - Inner container: `max-w-[800px] mx-auto text-center` with section padding via inline style
   - Headline: `text-section` with inline style `color: #FFFFFF;`. Exact PRD copy: `84% of people haven't started. The 0.04% who build with AI agents aren't waiting for you to catch up.`
   - CTA: Import Button. Cannot use variant="cta" directly because the Button component uses `bg-primary` which changes with theme. Instead, use Button with `variant="cta"` and add a wrapping `<div>` with `mt-8`, then add a `<style>` block that forces the button in this section to use `background: #5C3B9C; color: #FFFFFF;` and hover `background: #4E3285;` — OR simply create a raw `<a>` tag styled directly: `inline-flex items-center justify-center font-sans font-semibold rounded-lg h-12 px-8 text-body-lg transition-all duration-150` with inline style `background: #5C3B9C; color: #FFFFFF;` and hover via CSS. The simplest approach: use a scoped `<style>` block for the section that overrides button colors.
   - Link: `https://tidycal.com/garyctate/15-minute-meeting`, `target="_blank" rel="noopener noreferrer"`

3. Create `src/components/sections/TransitionalCTAsSection.astro`:
   - `id="section-transitional"`
   - Container: `max-w-[1200px] mx-auto` with section padding via inline style
   - Grid: `grid grid-cols-2 gap-6`, mobile (`max-md:`): `grid-cols-1`
   - Import BlogCard from `../BlogCard.astro`
   - Card 1: title="You're Not the Only One Stuck", date="" (empty — no date for curated homepage cards), excerpt="If AI feels like something for tech people, you're not alone. Start here.", href="/blog/youre-not-the-only-one-stuck"
   - Card 2: title="How Safe Is AI Really?", date="" (empty), excerpt="What you need to know before you start using AI in your business.", href="/blog/how-safe-is-ai-really"
   - Note: BlogCard expects a `date` prop. Pass an empty string — the BlogCard renders it in a `<p>` that will just be empty. If this looks bad, pass a placeholder like "Article" and adjust after visual check.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -l "section-why" src/components/sections/WhyConsultatesSection.astro && grep -l "section-cta" src/components/sections/StakesCTASection.astro && grep -l "section-transitional" src/components/sections/TransitionalCTAsSection.astro && grep "#0D1117" src/components/sections/StakesCTASection.astro</automated>
  </verify>
  <done>Why section shows 3 authority columns. Stakes CTA is always-dark with white text and purple button. Transitional CTAs shows 2 blog cards linking to blog posts.</done>
</task>

</tasks>

<verification>
All 5 section components exist in `src/components/sections/` with correct `id` attributes. Stakes CTA uses hardcoded dark colors. All copy matches PRD verbatim.
</verification>

<success_criteria>
- How It Works has 3 StepCards and a CTA button linking to TidyCal
- Social Proof has Frank Wiener quote with decorative `"` on secondary background
- Why Consultates has 3 columns with authority content
- Stakes CTA uses `#0D1117` background always, `#5C3B9C` button always — not theme tokens
- Transitional CTAs has 2 BlogCards linking to blog post URLs
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-02-SUMMARY.md`
</output>
