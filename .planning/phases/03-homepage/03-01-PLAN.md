---
phase: 03-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/components/sections/HeroSection.astro
  - src/components/sections/TrustBar.astro
  - src/components/sections/NegativeStakes.astro
  - src/components/sections/OfferingSection.astro
  - src/components/sections/UseCasesSection.astro
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05]

must_haves:
  truths:
    - "Hero section shows headline, paragraph, CTA button, and scroll indicator"
    - "Trust bar displays 4 credentials separated by middle dots"
    - "Negative Stakes shows 84% and 12-18 months stats on secondary background"
    - "Offering section shows 3 service cards linking to service pages"
    - "Use Cases section shows 2 testimonial blocks with context, quote, and attribution"
  artifacts:
    - path: "src/config.ts"
      provides: "Global constants (TIDYCAL_URL, CTA_TEXT, EMAIL, etc.)"
      exports: ["TIDYCAL_URL", "CTA_TEXT", "EMAIL", "LINKEDIN_URL", "SITE_URL"]
    - path: "src/components/sections/HeroSection.astro"
      provides: "Hero with headline, paragraph, CTA, scroll indicator"
      contains: "section-hero"
    - path: "src/components/sections/TrustBar.astro"
      provides: "Trust credentials bar"
      contains: "section-trust"
    - path: "src/components/sections/NegativeStakes.astro"
      provides: "Problem section with stats"
      contains: "section-stakes"
    - path: "src/components/sections/OfferingSection.astro"
      provides: "3 service cards"
      contains: "section-offering"
    - path: "src/components/sections/UseCasesSection.astro"
      provides: "2 testimonial blocks"
      contains: "section-cases"
  key_links:
    - from: "src/components/sections/HeroSection.astro"
      to: "src/components/Button.astro"
      via: "CTA button import"
      pattern: "import Button"
    - from: "src/components/sections/OfferingSection.astro"
      to: "src/components/ServiceCard.astro"
      via: "3 service card instances"
      pattern: "import ServiceCard"
    - from: "src/components/sections/NegativeStakes.astro"
      to: "src/components/StatBlock.astro"
      via: "2 stat block instances"
      pattern: "import StatBlock"
---

<objective>
Build the first 5 homepage sections (Hero through Use Cases) plus site config constants.

Purpose: Delivers the top half of the StoryBrand narrative — empathy, trust, problem, solution, and social proof in context. These sections compose the visitor's first impression and primary conversion path.

Output: 5 Astro section components in `src/components/sections/` and a `src/config.ts` with global constants.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-CONTEXT.md
@PRD.md (§4 Sections 2-6 for exact copy, CSS values, and layout specs)

<interfaces>
<!-- Existing components to import and use -->

From src/components/Button.astro:
```astro
interface Props {
  variant?: 'primary' | 'secondary' | 'ghost' | 'cta';
  size?: 'default' | 'large';
  href?: string;
  class?: string;
}
<!-- Usage: <Button variant="cta" href={url} target="_blank" rel="noopener noreferrer">Book a Free Call</Button> -->
```

From src/components/ScrollIndicator.astro:
```astro
<!-- No props. Usage: <ScrollIndicator /> -->
<!-- Position: absolute bottom-8, centered -->
```

From src/components/StatBlock.astro:
```astro
interface Props {
  number: string;
  label: string;
  source?: string;
}
```

From src/components/ServiceCard.astro:
```astro
interface Props {
  icon?: string;
  title: string;
  description: string;
  href: string;
}
<!-- Uses named slot "icon" for Phosphor SVG -->
```

From src/components/TestimonialBlock.astro:
```astro
interface Props {
  quote: string;
  name: string;
  title: string;
}
```

From src/layouts/Base.astro:
```astro
<!-- Nav and Footer already included. Page content goes in <slot />. -->
<!-- <main class="pt-16"> wraps the slot. -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create site config and Hero + Trust Bar sections</name>
  <files>src/config.ts, src/components/sections/HeroSection.astro, src/components/sections/TrustBar.astro</files>
  <action>
1. Create `src/config.ts` with global constants from PRD §1.3:
   ```ts
   export const TIDYCAL_URL = 'https://tidycal.com/garyctate/15-minute-meeting';
   export const CTA_TEXT = 'Book a Free Call';
   export const EMAIL = 'info@consultates.com';
   export const LINKEDIN_URL = 'https://www.linkedin.com/company/consultates-limited/';
   export const SITE_URL = 'https://consultates.com';
   ```

2. Create `src/components/sections/HeroSection.astro`:
   - `id="section-hero"`
   - Layout: `min-h-screen flex flex-col items-center justify-center text-center relative` with padding `clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem)` via inline style
   - Background: radial gradient via inline style with light/dark variants. Use a `<style>` block:
     - Light: `radial-gradient(ellipse at 50% 60%, #F6F2EB 0%, #FFFFFF 70%)`
     - Dark (`.dark` selector): `radial-gradient(ellipse at 50% 40%, rgba(92,59,156,0.15) 0%, #0D1117 60%)`
   - Content wrapper: `max-w-[800px]`
   - Headline (`text-hero text-foreground`): exact PRD copy `Clarity on AI — what to do, where to start, and who to trust`
   - Paragraph (`text-body-lg text-muted-foreground mt-6`): exact PRD copy (the "You're a leader..." paragraph)
   - CTA: Import Button, use `variant="cta"`, import TIDYCAL_URL from config, `target="_blank" rel="noopener noreferrer"`, class `mt-8`. Text: `Book a Free Call`
   - ScrollIndicator: Import and place after CTA, no wrapper needed (component already has absolute positioning)

3. Create `src/components/sections/TrustBar.astro`:
   - `id="section-trust"`
   - Layout: `flex items-center justify-center gap-2 flex-wrap text-center` with padding `1.5rem clamp(1.5rem, 5vw, 8rem)` via inline style
   - Content (`text-caption text-muted-foreground`): `Guest lecturer, Lead with AI PRO · 35 years across engineering, sales & AI · Trained executive cohorts through FlexOS · 16 countries`
   - Render as a single `<p>` element — the ` · ` separators are literal text content, no special markup needed
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && ls src/config.ts src/components/sections/HeroSection.astro src/components/sections/TrustBar.astro && grep -l "TIDYCAL_URL" src/config.ts && grep -l "section-hero" src/components/sections/HeroSection.astro && grep -l "section-trust" src/components/sections/TrustBar.astro</automated>
  </verify>
  <done>Config file exports all 5 constants. Hero section renders headline, paragraph, CTA linking to TidyCal, and scroll indicator. Trust bar shows 4 credentials separated by middle dots.</done>
</task>

<task type="auto">
  <name>Task 2: Create Negative Stakes and Offering sections</name>
  <files>src/components/sections/NegativeStakes.astro, src/components/sections/OfferingSection.astro</files>
  <action>
1. Create `src/components/sections/NegativeStakes.astro`:
   - `id="section-stakes"`
   - Full-bleed secondary background: wrap entire section in a `div` with `bg-secondary`
   - Inner container: `max-w-[1200px] mx-auto` with padding `clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem)` via inline style
   - Grid: `grid grid-cols-2 gap-12` on desktop, `grid-cols-1` at `max-md:` breakpoint (768px)
   - Import StatBlock from `../StatBlock.astro`
   - Left column: `<StatBlock number="84%" label="of the world hasn't used AI at all. Most leaders are still at zero." />`
   - Right column: `<StatBlock number="12–18 months" label="Until white collar computer tasks are fully automated, according to the CEO of Microsoft AI. The window to get ahead of this is closing." />`
   - Source line below grid: centered `text-cite text-muted-foreground mt-8 text-center` — exact PRD copy: `Source: DataReportal Digital 2026 · Mustafa Suleiman, Everyday AI Podcast, Feb 2026`

2. Create `src/components/sections/OfferingSection.astro`:
   - `id="section-offering"`
   - Container: `max-w-[1200px] mx-auto` with section padding via inline style
   - Heading (`text-section text-foreground`): `Three ways we help`
   - Paragraph (`text-body-lg text-muted-foreground max-w-[700px] mb-8`): exact PRD copy (the "Whether you're a leader..." paragraph)
   - Cards grid: `grid grid-cols-3 gap-6` on desktop, `md:grid-cols-2` at tablet (768-1023px with third card full width), `max-md:grid-cols-1` on mobile
   - Import ServiceCard from `../ServiceCard.astro`
   - 3 ServiceCards with inline SVGs in the `icon` named slot (Phosphor duotone, 32px, weight duotone):
     - Card 1: UserFocus icon, title "AI Coaching for Leaders", description from PRD, href `/services/ai-coaching-for-leaders`
     - Card 2: UsersThree icon, title "AI Training for Teams", description from PRD, href `/services/ai-training-for-teams`
     - Card 3: Briefcase icon, title "Fractional Exec Support", description from PRD, href `/services/fractional-exec-support`
   - For Phosphor inline SVGs: copy the SVG paths from Phosphor icons website. Use `class="w-8 h-8"` and `fill="currentColor"` for theme-aware coloring. The ServiceCard already wraps the icon slot in `text-primary`.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -l "section-stakes" src/components/sections/NegativeStakes.astro && grep -l "section-offering" src/components/sections/OfferingSection.astro && grep -c "ServiceCard" src/components/sections/OfferingSection.astro | grep -q "3" && echo "OK"</automated>
  </verify>
  <done>Negative Stakes shows 2 stat blocks on secondary background with source citation. Offering shows 3 service cards linking to their respective service pages with Phosphor icons.</done>
</task>

<task type="auto">
  <name>Task 3: Create Use Cases section</name>
  <files>src/components/sections/UseCasesSection.astro</files>
  <action>
Create `src/components/sections/UseCasesSection.astro`:
   - `id="section-cases"`
   - Container: `max-w-[800px] mx-auto` with section padding via inline style
   - Two stacked blocks. Each block: `py-8 border-b border-border` (last block: no border, use `last:border-b-0` or just omit on the second)
   - Each block has 3 elements:
     a. Context paragraph: `text-body text-foreground mb-6`
     b. Quote: `text-body-lg font-serif italic text-foreground border-l-[3px] border-primary pl-6 mb-4`
     c. Attribution: `text-caption font-semibold text-muted-foreground`
   - Do NOT use TestimonialBlock component here because the Use Cases blocks include a "context" paragraph above the quote that TestimonialBlock doesn't support. Build inline markup instead. (CONTEXT.md says "Uses TestimonialBlock" but the PRD §4 Section 6 specifies context + quote + attribution — a different structure. The context paragraph has no slot in TestimonialBlock. Build custom markup matching PRD exactly.)
   - Block 1 content: All copy verbatim from PRD §4 Section 6 — context about managing partner, the full quote, attribution "— Managing Partner, GenerativeLeads AI"
   - Block 2 content: All copy verbatim from PRD §4 Section 6 — context about director, the full quote, attribution "— Director Asia Pacific, Ace Pacific Group"
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -l "section-cases" src/components/sections/UseCasesSection.astro && grep -c "blockquote\|border-l" src/components/sections/UseCasesSection.astro</automated>
  </verify>
  <done>Use Cases section shows 2 testimonial blocks, each with context paragraph, border-left quote, and attribution. All copy matches PRD verbatim.</done>
</task>

</tasks>

<verification>
All 5 section components exist in `src/components/sections/` with correct `id` attributes. Config file exports all global constants. Components use existing shared components (Button, StatBlock, ServiceCard, ScrollIndicator) correctly.
</verification>

<success_criteria>
- `src/config.ts` exports TIDYCAL_URL, CTA_TEXT, EMAIL, LINKEDIN_URL, SITE_URL
- Hero section has headline, paragraph, CTA button with TidyCal link, and ScrollIndicator
- Trust bar shows 4 credentials separated by ` · `
- Negative Stakes has 2 StatBlocks on secondary background with source line
- Offering has 3 ServiceCards with Phosphor icons linking to service pages
- Use Cases has 2 testimonial blocks with context, quote, and attribution
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-01-SUMMARY.md`
</output>
