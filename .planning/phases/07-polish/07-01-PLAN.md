---
phase: 07-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Base.astro
  - src/pages/blog/[...slug].astro
  - public/robots.txt
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - DEPLOY-03

must_haves:
  truths:
    - "Every page has a unique title and meta description in the HTML head"
    - "Every page has a canonical URL link tag pointing to its full URL"
    - "Sitemap XML is generated on build and includes all public routes"
    - "robots.txt allows all crawlers and references the sitemap"
    - "Favicon appears in browser tabs across all pages"
    - "OG meta tags provide title, description, and image for social sharing"
  artifacts:
    - path: "src/layouts/Base.astro"
      provides: "Canonical URL and OG meta tags in head"
      contains: "rel=\"canonical\""
    - path: "public/robots.txt"
      provides: "Crawler permissions and sitemap reference"
      contains: "Sitemap:"
    - path: "public/favicon.png"
      provides: "Favicon for browser tabs"
  key_links:
    - from: "src/layouts/Base.astro"
      to: "Astro.site + Astro.url.pathname"
      via: "canonical URL construction"
      pattern: "Astro\\.site.*Astro\\.url"
    - from: "public/robots.txt"
      to: "sitemap-index.xml"
      via: "Sitemap directive"
      pattern: "Sitemap:.*sitemap"
---

<objective>
Add SEO meta tags, canonical URLs, OG tags, robots.txt, and verify sitemap generation and favicon. All pages already exist with correct titles and descriptions from prior phases — this plan adds the remaining production-readiness polish.

Purpose: Make the site discoverable by search engines and presentable when shared on social media.
Output: Base layout with canonical + OG tags, robots.txt, verified sitemap generation.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish/07-CONTEXT.md

<interfaces>
<!-- Base layout already accepts title and description props, has favicon link tag -->
From src/layouts/Base.astro:
```typescript
interface Props {
  title: string;
  description?: string;
}
```
Line 19: `<link rel="icon" type="image/png" href="/favicon.png" />`
Line 21: `<title>{title}</title>`
Line 22: `<meta name="description" content={description} />`

Astro config already has:
- `site: 'https://consultates.com'`
- `integrations: [react(), sitemap()]`

Existing assets:
- `public/favicon.png` (already in place from Phase 1 ASSET-03)
- `public/images/consultates-head.png` (head mark for OG fallback image)

All pages already pass title and description to Base layout. Blog post slug page uses `--` separator instead of em dash `—` — fix to match PRD convention.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical URL, OG meta tags, and fix blog title separator in Base layout</name>
  <files>src/layouts/Base.astro, src/pages/blog/[...slug].astro</files>
  <action>
In `src/layouts/Base.astro`, add the following to the `<head>` section after the existing `<meta name="description">` tag:

1. Canonical URL tag:
```html
<link rel="canonical" href={new URL(Astro.url.pathname, Astro.site).href} />
```

2. OG meta tags (using existing title/description props + head mark as fallback image per CONTEXT.md discretion):
```html
<meta property="og:type" content="website" />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:url" content={new URL(Astro.url.pathname, Astro.site).href} />
<meta property="og:image" content={new URL('/images/consultates-head.png', Astro.site).href} />
<meta property="og:site_name" content="Consultates" />
```

3. Twitter card meta:
```html
<meta name="twitter:card" content="summary" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
```

In `src/pages/blog/[...slug].astro`, fix the title separator from `--` to the em dash used by all other pages: change `${post.data.title} -- Consultates Blog` to `${post.data.title} — Consultates Blog`.

Do NOT add structured data / JSON-LD (deferred per CONTEXT.md).
Do NOT add OG image generation (deferred to v2 per CONTEXT.md).
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -20 && grep -c 'rel="canonical"' src/layouts/Base.astro && grep -c 'og:title' src/layouts/Base.astro</automated>
  </verify>
  <done>Base layout contains canonical URL tag and OG meta tags. Blog post title uses em dash separator. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create robots.txt and verify sitemap generation</name>
  <files>public/robots.txt</files>
  <action>
Create `public/robots.txt` with the following content:
```
User-agent: *
Allow: /

Sitemap: https://consultates.com/sitemap-index.xml
```

Then run `pnpm build` and verify:
1. `dist/sitemap-index.xml` exists (generated by @astrojs/sitemap)
2. `dist/robots.txt` exists (copied from public/)
3. Sitemap includes all public routes: /, /about, /contact, /blog, /services/ai-coaching-for-leaders, /services/ai-training-for-teams, /services/fractional-exec-support, /privacy-policy, and blog post pages
4. 404 page is NOT in the sitemap (correct behavior)

Also verify favicon.png is in dist/ output (confirming DEPLOY-03 from Phase 1 still intact).
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5 && test -f dist/robots.txt && echo "robots.txt OK" && test -f dist/sitemap-index.xml && echo "sitemap OK" && test -f dist/favicon.png && echo "favicon OK" && cat dist/robots.txt</automated>
  </verify>
  <done>robots.txt exists in build output with correct content. Sitemap XML is auto-generated with all public routes. Favicon is present in build output.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with no errors
2. Every HTML page in `dist/` contains `<link rel="canonical"` tag
3. Every HTML page in `dist/` contains `<meta property="og:title"` tag
4. `dist/robots.txt` contains `Sitemap: https://consultates.com/sitemap-index.xml`
5. `dist/sitemap-index.xml` exists and references sitemap files
6. `dist/favicon.png` exists
</verification>

<success_criteria>
- All 5 requirements (SEO-01 through SEO-04, DEPLOY-03) are satisfied
- Build produces a complete dist/ with sitemap, robots.txt, and favicon
- Every page has canonical URL and OG tags in rendered HTML
- No content or layout changes — purely additive head metadata
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-01-SUMMARY.md`
</output>
