---
phase: 06-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/config.ts
  - src/pages/blog/index.astro
  - src/pages/blog/[...slug].astro
  - src/styles/global.css
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02
  - BLOG-03

must_haves:
  truths:
    - "Content collections schema validates blog post frontmatter (title, date, author, excerpt, draft)"
    - "Blog index page renders published posts as BlogCard grid — 2 columns desktop, 1 column mobile"
    - "Blog post page renders Markdown content with prose styling, date, and reading time"
    - "Draft posts are filtered out of the blog index"
  artifacts:
    - path: "src/content/config.ts"
      provides: "Blog collection schema with zod validation"
      contains: "defineCollection"
    - path: "src/pages/blog/index.astro"
      provides: "Blog index page with card grid"
      contains: "getCollection"
    - path: "src/pages/blog/[...slug].astro"
      provides: "Dynamic blog post page with prose layout"
      contains: "getStaticPaths"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/components/BlogCard.astro"
      via: "import and render per post"
      pattern: "BlogCard"
    - from: "src/pages/blog/[...slug].astro"
      to: "src/content/config.ts"
      via: "getCollection('blog')"
      pattern: "getCollection.*blog"
---

<objective>
Build the blog infrastructure: content collections config, blog index page, and blog post layout.

Purpose: Establish the blog system so posts can be added as Markdown files and automatically appear on the index with proper rendering.
Output: Working blog index and post pages ready for content.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-blog/06-CONTEXT.md

<interfaces>
<!-- Existing BlogCard component the index page will use -->

From src/components/BlogCard.astro:
```astro
---
interface Props {
  title: string;
  date: string;
  excerpt: string;
  href: string;
}
---
```

From src/layouts/Base.astro:
```astro
---
interface Props {
  title: string;
  description?: string;
}
---
```

From src/pages/privacy-policy.astro (prose layout pattern to follow):
```astro
<section style="padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem);">
  <div style="max-width: 720px; margin: 0 auto;">
    <!-- content -->
  </div>
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content collections config and typography plugin wiring</name>
  <files>src/content/config.ts, src/styles/global.css</files>
  <action>
1. Create `src/content/config.ts` with a `blog` collection:
   - `title`: z.string()
   - `date`: z.date()
   - `author`: z.string().default('Gary Tate')
   - `excerpt`: z.string().max(200).optional()
   - `draft`: z.boolean().default(false)
   - Use `defineCollection` and `z` from `astro:content`
   - Export `collections = { blog }`

2. In `src/styles/global.css`, add the `@tailwindcss/typography` plugin import at the top (after `@import "tailwindcss"`):
   ```css
   @plugin "@tailwindcss/typography";
   ```
   Then add prose customizations to set Plex Serif for headings and Plex Sans for body within `.prose`:
   ```css
   .prose h1, .prose h2, .prose h3, .prose h4 {
     font-family: var(--font-serif);
   }
   .prose {
     font-family: var(--font-sans);
   }
   ```
   Place these after the existing custom utility classes. Use the existing font CSS custom properties already defined in the file.

Note: `@tailwindcss/typography` is already in devDependencies — no install needed.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && test -f src/content/config.ts && grep "defineCollection" src/content/config.ts && grep "@tailwindcss/typography" src/styles/global.css && echo "PASS"</automated>
  </verify>
  <done>Content collection schema exists with all 5 fields. Typography plugin is imported and prose font overrides are in global.css.</done>
</task>

<task type="auto">
  <name>Task 2: Blog index page and blog post layout</name>
  <files>src/pages/blog/index.astro, src/pages/blog/[...slug].astro</files>
  <action>
1. Create `src/pages/blog/index.astro`:
   - Import Base layout and BlogCard component
   - Use `getCollection('blog')` from `astro:content`
   - Filter out drafts: `.filter(post => !post.data.draft)`
   - Sort by date descending: `.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())`
   - Meta: title="Blog -- Consultates", description="Articles on AI adoption, AI safety, and practical guidance for business leaders."
   - Page heading: `<h1 class="text-hero mb-8">Blog</h1>`
   - Grid layout using inline style matching project pattern: `display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem;`
   - Add a `@media (max-width: 767px)` scoped style to set `grid-template-columns: 1fr`
   - Wrap in section with same padding as privacy page: `padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem);`
   - Container: `max-width: 960px; margin: 0 auto;` (wider than post layout to fit 2-col grid)
   - Each post renders a `<BlogCard>` with:
     - `title={post.data.title}`
     - `date={post.data.date.toLocaleDateString('en-GB', { year: 'numeric', month: 'long', day: 'numeric' })}`
     - `excerpt={post.data.excerpt || post.body?.slice(0, 160) + '...'}`
     - `href={'/blog/' + post.id}`

2. Create `src/pages/blog/[...slug].astro`:
   - Import Base layout
   - Use `getStaticPaths` with `getCollection('blog')` — map each post to `{ params: { slug: post.id }, props: { post } }`
   - In the template:
     - Use Base layout with title=`{post.data.title} -- Consultates Blog` and description={post.data.excerpt or first 160 chars}
     - Same section/container pattern as privacy page: `padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem);` and `max-width: 720px; margin: 0 auto;`
     - Post header: `<h1 class="text-hero">{post.data.title}</h1>`
     - Date + reading time line: `<p class="text-cite text-muted-foreground mb-8">`
     - Reading time: calculate from `post.body` word count divided by 200, rounded up, formatted as "{N} min read"
     - Add a "Back to Blog" link above the title: `<a href="/blog" class="text-body text-primary hover:underline mb-4 inline-block">&larr; Back to Blog</a>`
     - Render post content using `<Content />` from `post.render()` wrapped in `<div class="prose prose-lg max-w-none">`
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && test -f src/pages/blog/index.astro && test -f "src/pages/blog/[...slug].astro" && grep "getCollection" src/pages/blog/index.astro && grep "getStaticPaths" "src/pages/blog/[...slug].astro" && echo "PASS"</automated>
  </verify>
  <done>Blog index renders BlogCard grid filtered by draft status, sorted newest first. Blog post layout renders with prose styling, date, reading time, and back link. Both pages use Base layout with correct meta tags.</done>
</task>

</tasks>

<verification>
- `src/content/config.ts` exports blog collection with correct schema
- `src/pages/blog/index.astro` filters drafts, sorts by date, renders BlogCard grid
- `src/pages/blog/[...slug].astro` renders post content with prose styling and reading time
- Typography plugin wired in global.css with font overrides
</verification>

<success_criteria>
Blog infrastructure is complete: content collection schema validates posts, index page renders a card grid, post pages render Markdown with proper typography. Ready for blog post content files.
</success_criteria>

<output>
After completion, create `.planning/phases/06-blog/06-01-SUMMARY.md`
</output>
