---
phase: 06-blog
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/content/blog/how-safe-is-ai-really.md
  - src/content/blog/youre-not-the-only-one-stuck.md
  - src/content/blog/get-out-of-the-ai-cage.md
autonomous: true
requirements:
  - BLOG-04

must_haves:
  truths:
    - "All 3 blog posts are accessible via the blog index page"
    - "Each post renders with readable prose, correct title, date, and reading time"
    - "Homepage blog card links (/blog/youre-not-the-only-one-stuck and /blog/how-safe-is-ai-really) resolve to real posts"
    - "Draft posts do not appear on the blog index"
  artifacts:
    - path: "src/content/blog/how-safe-is-ai-really.md"
      provides: "AI safety blog post"
      contains: "title:"
    - path: "src/content/blog/youre-not-the-only-one-stuck.md"
      provides: "AI adoption blog post"
      contains: "title:"
    - path: "src/content/blog/get-out-of-the-ai-cage.md"
      provides: "AI cage blog post"
      contains: "title:"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/content/blog/*.md"
      via: "getCollection('blog') returns all 3 posts"
      pattern: "getCollection.*blog"
    - from: "src/pages/index.astro"
      to: "src/content/blog/youre-not-the-only-one-stuck.md"
      via: "BlogCard href /blog/youre-not-the-only-one-stuck"
      pattern: "youre-not-the-only-one-stuck"
---

<objective>
Migrate 3 blog posts from HTML source files to Markdown content collection files.

Purpose: Populate the blog with real content so the site launches with 3 published articles. These slugs are already linked from the homepage.
Output: 3 Markdown files in src/content/blog/ with correct frontmatter and clean prose.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-blog/06-CONTEXT.md
@.planning/phases/06-blog/06-01-SUMMARY.md

<interfaces>
<!-- Content collection schema from Plan 01 -->
Blog frontmatter schema:
```typescript
{
  title: z.string(),
  date: z.date(),
  author: z.string().default('Gary Tate'),
  excerpt: z.string().max(200).optional(),
  draft: z.boolean().default(false),
}
```

Slug mapping (from CONTEXT.md):
- ai-safety-and-security.html -> how-safe-is-ai-really.md
- ai-leaving-you-behind.html -> youre-not-the-only-one-stuck.md
- get-out-of-the-ai-cage.html -> get-out-of-the-ai-cage.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert 3 HTML blog posts to Markdown</name>
  <files>src/content/blog/how-safe-is-ai-really.md, src/content/blog/youre-not-the-only-one-stuck.md, src/content/blog/get-out-of-the-ai-cage.md</files>
  <action>
For each of the 3 HTML source files in `~/.openclaw/Mi6-IQ/projects/consultates-rebuild/html/`:

1. Read the HTML file
2. Extract the article title, publication date, and body content
3. Strip all WebWave layout markup, navigation, footer, scripts, and styling — keep only the article prose
4. Convert HTML body to clean Markdown:
   - `<h2>` -> `## `, `<h3>` -> `### `
   - `<p>` -> paragraphs separated by blank lines
   - `<strong>` -> `**bold**`, `<em>` -> `*italic*`
   - `<ul>/<li>` -> `- ` list items
   - `<ol>/<li>` -> `1. ` numbered items
   - `<a href>` -> `[text](url)`
   - `<blockquote>` -> `> `
   - Remove any image references (images are deferred to v2, CONT-04)
   - Remove any inline styles, class attributes, data attributes
5. Write each file with YAML frontmatter:

```yaml
---
title: "[extracted title]"
date: [extracted date as YYYY-MM-DD]
author: "Gary Tate"
excerpt: "[first ~150 chars of article body, clean sentence ending]"
draft: false
---
```

File mapping:
- `ai-safety-and-security.html` -> `src/content/blog/how-safe-is-ai-really.md`
- `ai-leaving-you-behind.html` -> `src/content/blog/youre-not-the-only-one-stuck.md`
- `get-out-of-the-ai-cage.html` -> `src/content/blog/get-out-of-the-ai-cage.md`

Ensure the Markdown reads as clean, well-structured prose. Do NOT modify or rewrite the content — preserve Gary's voice and wording exactly. Only transform the markup format.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && test -f src/content/blog/how-safe-is-ai-really.md && test -f src/content/blog/youre-not-the-only-one-stuck.md && test -f src/content/blog/get-out-of-the-ai-cage.md && grep "^title:" src/content/blog/*.md && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 blog posts exist as Markdown files with valid frontmatter. `pnpm build` succeeds with no content collection validation errors. Blog index shows 3 posts. Homepage links to /blog/youre-not-the-only-one-stuck and /blog/how-safe-is-ai-really resolve correctly.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- All 3 posts appear on the blog index page (not filtered as drafts)
- Each post page renders with correct title, date, reading time, and clean prose
- Homepage blog card links resolve to real post pages
</verification>

<success_criteria>
3 blog posts are published, accessible from the blog index and from homepage links. Content is clean Markdown with correct frontmatter matching the collection schema.
</success_criteria>

<output>
After completion, create `.planning/phases/06-blog/06-02-SUMMARY.md`
</output>
