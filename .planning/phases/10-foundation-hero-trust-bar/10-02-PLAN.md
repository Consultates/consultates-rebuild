---
phase: 10-foundation-hero-trust-bar
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/components/sections/HeroSection.astro
  - src/components/islands/HeroIsland.tsx
autonomous: false
requirements:
  - RDES-06
  - RDES-07
  - RDES-08
  - RDES-09
  - RDES-10
  - RDES-11
  - RDES-12

must_haves:
  truths:
    - "Hero has a dark background with dot grid overlay and radial purple glow behind headline"
    - "Hero headline is white, uses design system hero size, em tags render in accent purple"
    - "Hero CTA is pill-shaped with accent purple background and arrow that shifts on hover"
    - "Hero body text uses muted color and is capped at 580px"
    - "On mobile (below 768px) a poster image shows instead of video"
    - "Hero entrance animation delay is 1000ms (reduced from 1200ms)"
    - "Desktop/tablet/mobile screenshots confirm visual fidelity"
  artifacts:
    - path: "src/components/sections/HeroSection.astro"
      provides: "Dark hero with video background, dot grid, glow, content container"
      min_lines: 40
    - path: "src/components/islands/HeroIsland.tsx"
      provides: "Hero animation with updated timing, pill CTA, em accent support"
      min_lines: 100
  key_links:
    - from: "src/components/sections/HeroSection.astro"
      to: "src/styles/global.css"
      via: "design system color tokens and variables"
      pattern: "--hero-bg|--hero-fg|--hero-muted|--stakes-accent"
    - from: "src/components/islands/HeroIsland.tsx"
      to: "src/components/sections/HeroSection.astro"
      via: "React island hydration inside Astro section"
      pattern: "HeroIsland.*client:load"
---

<objective>
Redesign the hero section to match the marketing-design-guidelines.html spec -- dark cinematic hero with video background, dot grid, purple glow, pill CTA, and updated typography.

Purpose: The hero is the first thing visitors see. The v2 design transforms it from a light gradient background to a dark, cinematic section with depth (dot grid, glow) and a professional pill-shaped CTA.

Output: Redesigned HeroSection.astro and HeroIsland.tsx
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-hero-trust-bar/10-01-SUMMARY.md
@src/components/sections/HeroSection.astro
@src/components/islands/HeroIsland.tsx
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign hero section with dark background, video, glow, and pill CTA</name>
  <files>src/components/sections/HeroSection.astro, src/components/islands/HeroIsland.tsx</files>
  <action>
**HeroSection.astro -- Complete rewrite:**

Remove the old light gradient background. Build the new dark cinematic hero structure.

Template structure:
- `section#section-hero.c-hero` wrapping everything
- `video.c-hero-video` element: autoplay, muted, loop, playsinline, poster="/images/hero-poster.webp", aria-hidden="true". Source: "/video/hero-bg.mp4" type="video/mp4". NOTE: video file may not exist yet -- element degrades gracefully
- `div.c-hero-overlay` (dark overlay over video)
- `div.c-hero-content` containing the HeroIsland with these props:
  - tagline="AI Consulting"
  - headline: the existing headline text but with `<em>` wrapping "who to trust": `Clarity on AI — what to do, where to start, and <em>who to trust</em>`
  - paragraph: same text as current
  - ctaHref={TIDYCAL_URL}, ctaText="Book a Free Call"

Scoped styles block:
- `.c-hero`: background var(--hero-bg, #0D1117), color var(--hero-fg, #E2E0EC), padding clamp(6rem, 14vw, 10rem) 0 clamp(5rem, 12vw, 8rem), text-align center, position relative, overflow hidden, min-height calc(100vh - 4rem), display flex, align-items center, justify-content center
- `.c-hero-video`: position absolute, inset 0, width/height 100%, object-fit cover, filter grayscale(1), opacity 0.3, pointer-events none
- `@media (max-width: 767px)`: .c-hero-video display none; .c-hero gets background-image url('/images/hero-poster.webp'), background-size cover, background-position center
- `.c-hero-overlay`: position absolute, inset 0, background var(--hero-bg, #0D1117), opacity 0.75, pointer-events none
- `.c-hero::before` (glow): position absolute, top 40%, left 50%, transform translate(-50%, -50%), width 800px, height 500px, background radial-gradient(ellipse, rgba(92,59,156,0.12) 0%, rgba(92,59,156,0.04) 40%, transparent 70%), pointer-events none, z-index 1
- `.c-hero::after` (dots): position absolute, inset 0, background-image radial-gradient(circle, rgba(139,108,199,0.07) 1px, transparent 1px), background-size 32px 32px, pointer-events none, z-index 1
- `.c-hero-content`: position relative, z-index 2, max-width var(--content-max, 1140px), margin 0 auto, padding-left/right var(--content-px, clamp(1.5rem, 4vw, 2.5rem))

Global styles (using `:global()` wrapper so they reach into the React island):
- `:global(.c-hero-tagline)`: font-family var(--font-mono), font-size 0.8125rem, font-weight 500, color var(--stakes-accent, #8B6CC7), text-transform uppercase, letter-spacing 0.12em, margin-bottom 1.5rem
- `:global(.c-hero-h1)`: font-family var(--font-serif), font-size var(--text-hero, clamp(2.75rem, 5.5vw, 4rem)), line-height 1.08, letter-spacing -0.025em, font-weight 700, color #FFFFFF, max-width 820px, margin 0 auto 1.75rem
- `:global(.c-hero-h1 em)`: font-style normal, color var(--stakes-accent, #8B6CC7)
- `:global(.c-hero-body)`: font-size var(--text-body-lg, clamp(1.0625rem, 1.5vw, 1.25rem)), line-height 1.65, color var(--hero-muted, #7A7B90), max-width 580px, margin 0 auto 2.75rem
- `:global(.c-hero-cta)`: display inline-flex, align-items center, gap 0.625rem, padding 0.9375rem 2.25rem, background var(--stakes-accent, #8B6CC7), color #FFFFFF, font-family var(--font-sans), font-size 1rem, font-weight 600, border-radius 99px, text-decoration none, border none, cursor pointer, transition transform 0.2s box-shadow 0.2s
- `:global(.c-hero-cta:hover)`: transform translateY(-2px), box-shadow 0 8px 24px rgba(139,108,199,0.3)
- `:global(.c-hero-cta .arrow)`: transition transform 0.2s
- `:global(.c-hero-cta:hover .arrow)`: transform translateX(3px)

**HeroIsland.tsx -- Rewrite animation logic:**

1. Add `tagline` optional prop to HeroIslandProps interface.

2. Change initial delay from 1200ms to 1000ms (RDES-12). Update setTimeout to use 1000. Update headlineEndDelay base from 1.2 to 1.0.

3. Replace letter-stagger animation with whole-element fade-up sequence. The v2 design does NOT use per-character animation. Remove the `headline.split('').map()` pattern entirely.

4. Parse headline to support em tags safely (no innerHTML). Create a `parseHeadline` function that splits on `<em>` and `</em>` tags and returns an array of `{ text, isEm }` objects. Render: text parts as React `span` elements, em parts as React `em` elements with className "c-hero-h1-em" (no extra styling needed -- parent CSS handles it).

5. Animation timing (all Framer Motion, all delays in seconds):
   - Tagline: fadeUp, delay 0.2
   - Headline (h1): fadeUp, delay 0.4
   - Body (p): fadeUp, delay 0.8
   - CTA (div wrapping a tag): scaleIn, delay 1.2
   - Scroll indicator: fadeIn, delay 1.6

6. CTA markup: `<a>` with className="c-hero-cta", contains {ctaText} text and a `<span className="arrow" aria-hidden="true">` with the right arrow character.

7. Scroll indicator: keep the mouse-icon shape but change colors to white since hero is always dark. Use inline styles: border 2px solid rgba(255,255,255,0.6), dot background rgba(255,255,255,0.6). Keep the scroll-bob-dot className for the bounce animation.

8. Reduced motion path: render all elements immediately visible (no motion). Same markup structure (tagline, h1 with parsed em, body, pill CTA with arrow, scroll indicator) but no Framer Motion wrappers.

9. Remove all old Tailwind classes: text-hero, text-foreground, text-muted-foreground, bg-primary, text-primary-foreground, etc. All styling now via c-hero-* CSS classes defined in HeroSection.astro.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro build 2>&1 | tail -5 && grep -c "c-hero" src/components/sections/HeroSection.astro && grep -c "stakes-accent\|hero-muted\|hero-bg" src/components/sections/HeroSection.astro</automated>
  </verify>
  <done>Hero section has dark background with dot grid overlay and radial purple glow. Headline is white with em tags in accent purple. CTA is pill-shaped (border-radius 99px) with arrow hover. Body text is muted color, max-width 580px. Video element present (degrades gracefully). Mobile hides video. Animation delay is 1000ms. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of hero and trust bar</name>
  <action>Verify the complete design system foundation, hero redesign (dark bg, video, glow, dot grid, pill CTA, em accent), and trust bar as standalone section</action>
  <how-to-verify>
    1. Run `cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro dev` if not already running
    2. Open http://localhost:4321
    3. Verify hero section:
       - Dark background (near-black) with subtle purple radial glow behind headline
       - Dot grid pattern visible (small purple dots at 32px spacing)
       - "AI CONSULTING" tagline in mono uppercase purple text above headline
       - Headline in large white serif font, "who to trust" appears in purple accent
       - Body text in muted gray, narrower column than headline (580px max)
       - Pill-shaped purple CTA button "Book a Free Call" with right arrow
       - Hover CTA: button lifts up, arrow shifts right
       - Scroll indicator (mouse icon) at bottom with white border
    4. Verify trust bar (from plan 01):
       - Appears directly below the hero as its own full-width section
       - Shows 4 credential items separated by small dots
       - Text is small (13px), muted gray, centered
       - Visible on mobile (resize to narrow width to confirm)
    5. Resize to mobile (390px wide):
       - Video hidden, dark background still shows with glow and dots
       - Trust bar wraps credentials across lines
       - Hero text sizes down responsively
    6. Scroll down to verify nothing broke:
       - GSAP scroll-snap still works on desktop for remaining sections
       - Other sections render correctly
    7. Toggle dark mode: hero stays dark (slightly different shade), trust bar background adapts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx astro build` succeeds without errors
2. Hero has c-hero class with dark background, video element, overlay, glow and dot pseudo-elements
3. Hero uses design system tokens from plan 01
4. CTA has 99px border-radius and arrow element
5. GSAP scroll-snap on desktop still works for other sections
</verification>

<success_criteria>
- Dark cinematic hero with dot grid, purple glow, video element
- White headline with purple em accent at design system size
- Pill CTA with arrow hover animation
- Body text capped at 580px in muted color
- Mobile poster fallback (video hidden below 768px)
- Animation delay reduced to 1000ms
- Human approves visual fidelity
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-hero-trust-bar/10-02-SUMMARY.md`
</output>
