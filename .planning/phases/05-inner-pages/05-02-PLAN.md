---
phase: 05-inner-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/about.astro
  - src/pages/contact.astro
autonomous: true
requirements:
  - ABOUT-01
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "About page renders at /about with Gary's photo, hero headline, The Story, Family Business, Lead with AI PRO, and CTA"
    - "Contact page renders at /contact with two-column layout (info left, form right)"
    - "Contact form has Name (required), Email (required), Company (optional), Message (required) fields plus honeypot"
    - "Submitting the contact form shows 'Sending...' for 1 second then a success message replacing the form"
    - "Book a Free Call CTA on About page links to TidyCal URL"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "About page with 5 sections and full PRD copy"
      min_lines: 60
    - path: "src/pages/contact.astro"
      provides: "Contact page with info column and mock form"
      min_lines: 80
  key_links:
    - from: "src/pages/about.astro"
      to: "src/layouts/Base.astro"
      via: "Astro layout import"
      pattern: "import Base"
    - from: "src/pages/contact.astro"
      to: "src/components/FormInput.astro"
      via: "import for form fields"
      pattern: "import FormInput"
    - from: "src/pages/contact.astro"
      to: "src/components/Button.astro"
      via: "import for submit button"
      pattern: "import Button"
---

<objective>
Build the About and Contact pages with full PRD content.

Purpose: About page builds trust (Gary's credentials and story). Contact page is the secondary conversion path (form + direct links). Both are critical for visitors who want to learn more before booking.
Output: Two fully rendered pages using existing Base layout and form components.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@PRD.md (§5.2 About Page, §5.4 Contact Page)

<interfaces>
From src/layouts/Base.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```

From src/components/Button.astro:
```astro
interface Props {
  variant?: 'primary' | 'secondary' | 'ghost' | 'cta';
  size?: 'default' | 'large';
  href?: string;
  class?: string;
  [key: string]: unknown;
}
```

From src/components/FormInput.astro:
```astro
interface Props {
  label: string;
  name: string;
  type?: string;
  required?: boolean;
  error?: string;
  textarea?: boolean;
  placeholder?: string;
  disabled?: boolean;
}
```

From src/config.ts:
```ts
export const TIDYCAL_URL = 'https://tidycal.com/garyctate/15-minute-meeting';
export const CTA_TEXT = 'Book a Free Call';
export const EMAIL = 'info@consultates.com';
export const LINKEDIN_URL = 'https://www.linkedin.com/company/consultates-limited/';
```

Existing image: `public/images/gary-tate.webp` (already 400x400 WEBP from Phase 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create About page</name>
  <files>src/pages/about.astro</files>
  <action>
Create `src/pages/about.astro` using Base layout. Import Button and config constants.

- title: "About Gary Tate — Consultates"
- description: "Engineer. Commercial leader. AI practitioner. 35 years across military electronics, enterprise sales, and production AI — now helping leaders make sense of it."

All sections use homepage spacing: `padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem);` with `max-width: 1200px; margin: 0 auto;` inner container.

**Section 1 — Hero:**
- Gary's photo: `<img src="/images/gary-tate.webp" alt="Gary Tate" width="200" height="200" class="rounded-full object-cover" />` — centered or left-aligned above headline
- Headline (`text-hero`): "Engineer. Commercial leader. AI practitioner."
- Subline (`text-body-lg text-muted-foreground`): "35 years across military electronics, enterprise sales leadership, and production AI."

**Section 2 — The Story:**
- Heading (`text-section`): "The through-line"
- 5 paragraphs from PRD §5.2 verbatim (Gary started coding at 10... through ...all carry weight). Use `text-body text-muted-foreground` for paragraphs with `space-y-4` or similar spacing between them.

**Section 3 — Family Business:**
- Heading (`text-section`): "A family business"
- Paragraph from PRD verbatim: "Consultates is Gary and Trinh Tate. They don't scale through volume. They scale through trust. You get one accountable team from start to finish — not a rotating cast of consultants."

**Section 4 — Lead with AI PRO:**
- Heading (`text-section`): "Lead with AI PRO"
- Paragraph from PRD verbatim (Gary is a guest lecturer...).

**Section 5 — CTA:**
- Centered Button with `variant="cta"` href=TIDYCAL_URL target="_blank" rel="noopener noreferrer", text "Book a Free Call"
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>About page renders at /about with photo, all 5 sections, full PRD copy, and working CTA link.</done>
</task>

<task type="auto">
  <name>Task 2: Create Contact page with client-side mock form</name>
  <files>src/pages/contact.astro</files>
  <action>
Create `src/pages/contact.astro` using Base layout. Import FormInput, Button, and config constants.

- title: "Contact — Consultates"
- description: "Get in touch with Gary Tate. Book a free call, send a message, or connect on LinkedIn."

**Layout:** Two-column grid: `grid grid-cols-1 md:grid-cols-2 gap-12` (use gap-12 for generous spacing). Max-width 1200px, centered, with standard section padding. Single column on mobile.

**Left column:**
- Heading (`text-section`): "Let's talk"
- Paragraph (`text-body-lg text-muted-foreground`): "Whether you have a specific challenge or just want to explore how AI fits into your business — I'd like to hear from you. Book a call, send a message, or connect on LinkedIn."
- Direct links list (`text-body`, `space-y-2`):
  - "Email: info@consultates.com" → `mailto:info@consultates.com` (underline on hover)
  - "LinkedIn" → LINKEDIN_URL (target="_blank" rel="noopener noreferrer")
  - "Book a free call" → TIDYCAL_URL (target="_blank" rel="noopener noreferrer")

**Right column — Contact form:**
Use a `<form>` element with `id="contact-form"`. Fields using FormInput component:
1. FormInput: label="Name", name="name", required=true, placeholder="Your name"
2. FormInput: label="Email", name="email", type="email", required=true, placeholder="you@example.com"
3. FormInput: label="Company", name="company", placeholder="Your company (optional)"
4. FormInput: label="Message", name="message", textarea=true, required=true, placeholder="How can I help?"

Honeypot field (hidden): `<input type="text" name="website" class="sr-only" tabindex="-1" autocomplete="off" />`

Submit button: `<Button variant="primary" type="submit">Send Message</Button>` (use the button element variant, not href)

**Success message div** (hidden by default, id="contact-success"):
`background: var(--secondary); border: 1px solid var(--border); border-radius: 0.75rem; padding: 1rem; color: var(--foreground);`
Text: "Thanks for your message. I'll get back to you within 24 hours."

**Inline `<script>` for Phase 1 mock behavior:**
```js
const form = document.getElementById('contact-form');
const success = document.getElementById('contact-success');

form?.addEventListener('submit', (e) => {
  e.preventDefault();

  // Check honeypot
  const honeypot = form.querySelector('input[name="website"]');
  if (honeypot?.value) return;

  // Basic validation
  const name = form.querySelector('input[name="name"]');
  const email = form.querySelector('input[name="email"]');
  const message = form.querySelector('textarea[name="message"]');

  if (name?.value.length < 2 || name?.value.length > 100) return;
  if (!email?.validity.valid) return;
  if (message?.value.length < 10 || message?.value.length > 5000) return;

  // Disable submit, show sending
  const btn = form.querySelector('button[type="submit"]');
  if (btn) {
    btn.disabled = true;
    btn.textContent = 'Sending...';
  }

  // After 1s, hide form, show success
  setTimeout(() => {
    form.style.display = 'none';
    if (success) success.style.display = 'block';
  }, 1000);
});
```

The success div starts with `style="display: none;"`.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Contact page renders at /contact with two-column layout, 4 form fields + honeypot, mock submission showing success message after 1s delay.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes with no errors
- Built output includes `about/index.html` and `contact/index.html`
- About page contains Gary's photo, all 5 section headings, and CTA
- Contact page contains form with 4 visible fields, honeypot, and submit button
</verification>

<success_criteria>
About page shows Gary's full story with photo and CTA. Contact page has working mock form that shows success message on submission. Both use correct PRD copy verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/05-inner-pages/05-02-SUMMARY.md`
</output>
