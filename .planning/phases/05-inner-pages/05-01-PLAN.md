---
phase: 05-inner-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/ServicePage.astro
  - src/pages/services/ai-coaching-for-leaders.astro
  - src/pages/services/ai-training-for-teams.astro
  - src/pages/services/fractional-exec-support.astro
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-03

must_haves:
  truths:
    - "AI Coaching page renders at /services/ai-coaching-for-leaders with hero, what you get, who it's for, how it works, and CTA"
    - "AI Training page renders at /services/ai-training-for-teams with hero, what you get, who it's for, how it works, and CTA"
    - "Fractional Exec page renders at /services/fractional-exec-support with hero, what you get, who it's for, how it works, and CTA"
    - "How It Works section on each service page shows the same 3 steps as homepage Section 7"
    - "Book a Free Call CTA on each page links to TidyCal URL"
  artifacts:
    - path: "src/layouts/ServicePage.astro"
      provides: "Shared service page template with 5 sections"
      min_lines: 40
    - path: "src/pages/services/ai-coaching-for-leaders.astro"
      provides: "AI Coaching page with PRD copy"
      min_lines: 20
    - path: "src/pages/services/ai-training-for-teams.astro"
      provides: "AI Training page with PRD copy"
      min_lines: 20
    - path: "src/pages/services/fractional-exec-support.astro"
      provides: "Fractional Exec page with PRD copy"
      min_lines: 20
  key_links:
    - from: "src/pages/services/*.astro"
      to: "src/layouts/ServicePage.astro"
      via: "Astro layout import"
      pattern: "layout.*ServicePage"
    - from: "src/layouts/ServicePage.astro"
      to: "src/components/StepCard.astro"
      via: "import for How It Works section"
      pattern: "import StepCard"
    - from: "src/layouts/ServicePage.astro"
      to: "src/components/Button.astro"
      via: "import for CTA buttons"
      pattern: "import Button"
---

<objective>
Build all three service pages using a shared Astro layout template.

Purpose: Service pages are the primary conversion path from the homepage offering section — visitors click a service card and need to see details + book a call.
Output: Three fully rendered service pages with all PRD copy, reusing existing StepCard and Button components.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@PRD.md (§5.1 Service Pages)

<interfaces>
<!-- Existing components the executor will use -->

From src/layouts/Base.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```
Base layout wraps all pages with Nav, Footer, theme flash prevention, GSAP script.

From src/components/Button.astro:
```astro
interface Props {
  variant?: 'primary' | 'secondary' | 'ghost' | 'cta';
  size?: 'default' | 'large';
  href?: string;
  class?: string;
  [key: string]: unknown;
}
```

From src/components/StepCard.astro:
```astro
interface Props {
  number: number;
  heading: string;
  description: string;
}
```

From src/config.ts:
```ts
export const TIDYCAL_URL = 'https://tidycal.com/garyctate/15-minute-meeting';
export const CTA_TEXT = 'Book a Free Call';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared ServicePage layout and AI Coaching page</name>
  <files>src/layouts/ServicePage.astro, src/pages/services/ai-coaching-for-leaders.astro</files>
  <action>
Create `src/layouts/ServicePage.astro` — a shared Astro layout for all three service pages. It accepts props and renders 5 sections:

**Props interface:**
```astro
interface Props {
  title: string;          // page <title>
  description: string;    // meta description
  headline: string;       // text-hero heading
  subline: string;        // text-body-lg muted subline
  whatYouGet: string;     // HTML string for What You Get section (2 paragraphs)
  whoItsFor: string;      // HTML string for Who It's For section
}
```

**Layout structure (all sections use homepage spacing):**
Each section: `padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem);` with `max-width: 1200px; margin: 0 auto;` inner container.

1. **Hero:** headline in `text-hero`, subline in `text-body-lg text-muted-foreground`
2. **What You Get:** heading "What you get" in `text-section`, body paragraphs in `text-body` (use `set:html` for the prop)
3. **Who It's For:** heading "Who it's for" in `text-sub`, body in `text-body` (use `set:html`)
4. **How It Works:** heading "How it works" in `text-section`, then the same 3 StepCard grid from homepage (3-col on md, 1-col mobile, gap-8):
   - Step 1: "We talk" / "Tell me where you are and what you're trying to figure out. No preparation needed."
   - Step 2: "You get a plan" / "Specific to your business, your team, and your goals. Not a template."
   - Step 3: "We do the work together" / "Coaching, training, or hands-on leadership. One accountable team, start to finish."
5. **CTA:** centered `Button` with `variant="cta"` href=TIDYCAL_URL, text "Book a Free Call", target="_blank" rel="noopener noreferrer"

Import Base layout, StepCard, Button, TIDYCAL_URL from config.

Then create `src/pages/services/ai-coaching-for-leaders.astro` using ServicePage layout with PRD §5.1 copy verbatim:
- title: "AI Coaching for Leaders — Consultates"
- description: "One-to-one AI coaching sessions for business leaders. You bring your situation, you leave with a plan you can act on."
- headline: "AI Coaching for Leaders"
- subline: "One-to-one sessions. You bring your real situation — you leave with a plan you can act on."
- whatYouGet: The two paragraphs from PRD (Gary has sat across... / Each session starts with...)
- whoItsFor: "Business leaders, founders, fractional executives, solopreneurs, consultants — anyone who needs clarity on AI without the jargon. You don't need to be technical. You just need to be ready to start."
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>ServicePage layout created. AI Coaching page renders at /services/ai-coaching-for-leaders with all 5 sections and correct PRD copy.</done>
</task>

<task type="auto">
  <name>Task 2: Create AI Training and Fractional Exec service pages</name>
  <files>src/pages/services/ai-training-for-teams.astro, src/pages/services/fractional-exec-support.astro</files>
  <action>
Create `src/pages/services/ai-training-for-teams.astro` using ServicePage layout with PRD §5.1 copy verbatim:
- title: "AI Training for Teams — Consultates"
- description: "Hands-on AI workshops built around your team's actual work. Design thinking approach — people learn by doing."
- headline: "AI Training for Teams"
- subline: "Hands-on workshops built around your team's actual work. Design thinking approach — people learn by doing."
- whatYouGet: The two paragraphs (The curriculum comes from... / Every workshop is built around...)
- whoItsFor: "Teams told to \"use AI\" but given no guidance. Sales teams, marketing teams, operations teams — anyone doing knowledge work. Works for groups of 5 to 50."

Create `src/pages/services/fractional-exec-support.astro` using ServicePage layout with PRD §5.1 copy verbatim:
- title: "Fractional Executive Support — Consultates"
- description: "Senior AI go-to-market leadership without a full-time hire. Strategy, product direction, and execution."
- headline: "Fractional Executive Support"
- subline: "Senior AI go-to-market leadership without a full-time hire."
- whatYouGet: The two paragraphs (Gary has been a CRO... / This isn't advisory from a distance...)
- whoItsFor: "Startups and scale-ups that need experienced GTM leadership for AI products but aren't ready for (or don't need) a full-time exec hire. Companies entering new markets, launching AI products, or building their first sales organisation."

Both pages follow exact same pattern as AI Coaching page from Task 1.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 service pages build and render at their correct routes with full PRD copy.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes with no errors
- Built output includes `services/ai-coaching-for-leaders/index.html`, `services/ai-training-for-teams/index.html`, `services/fractional-exec-support/index.html`
- Each page contains the correct headline, subline, What You Get, Who It's For, How It Works (3 steps), and CTA
</verification>

<success_criteria>
Three service pages render with all PRD §5.1 copy, shared layout template, reused StepCard and Button components, and correct TidyCal CTA links.
</success_criteria>

<output>
After completion, create `.planning/phases/05-inner-pages/05-01-SUMMARY.md`
</output>
