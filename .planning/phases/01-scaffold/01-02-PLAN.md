---
phase: 01-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/styles/global.css
  - src/layouts/Base.astro
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "Royal Purple and all brand color tokens render correctly in light and dark modes"
    - "No flash of wrong theme on page load"
    - "IBM Plex Sans, Serif, and Mono load from local files with no external font requests"
    - "All custom type scale classes (text-hero, text-section, text-sub, text-body-lg, text-body, text-caption, text-stats, text-cite) apply correctly"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Tailwind 4 @theme block, @font-face declarations, :root and .dark token definitions, type scale utilities"
      contains: "@theme"
      min_lines: 100
    - path: "src/layouts/Base.astro"
      provides: "Base HTML layout with theme flash script, font preloads, global CSS import, and slot"
      contains: "localStorage"
      min_lines: 30
  key_links:
    - from: "src/layouts/Base.astro"
      to: "src/styles/global.css"
      via: "CSS import"
      pattern: "import.*global\\.css"
    - from: "src/styles/global.css"
      to: "public/fonts/"
      via: "@font-face src url()"
      pattern: "url\\(/fonts/IBMPlex"
    - from: "src/layouts/Base.astro"
      to: "public/fonts/"
      via: "preload link tags"
      pattern: "rel=\"preload\".*fonts/IBMPlex"
    - from: "src/styles/global.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: "@import \"tailwindcss\""
---

<objective>
Create the complete design system CSS (Tailwind 4 tokens, font-face declarations, type scale) and the base HTML layout with theme flash prevention.

Purpose: Every page and component built in later phases inherits correct colors, fonts, and theme behavior automatically through this foundation.
Output: A working design system rendered on a test page with correct light/dark theme switching.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-CONTEXT.md
@.planning/phases/01-scaffold/01-01-SUMMARY.md
@PRD.md (sections 2.1, 2.2, 2.8, 6.10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global CSS with Tailwind 4 tokens, font-face, and type scale</name>
  <files>src/styles/global.css</files>
  <action>
Create `src/styles/global.css` with the complete design system. Build it in this exact order:

**1. Tailwind import:**
```css
@import "tailwindcss";
```

**2. @theme block** — copy verbatim from PRD section 2.8:
```css
@theme {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-primary: var(--primary);
  --color-primary-hover: var(--primary-hover);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-border: var(--border);
  --color-ring: var(--ring);
  --color-destructive: var(--destructive);
  --color-icon-duotone: var(--icon-duotone);
  --color-footer-bg: var(--footer-bg);
  --color-footer-text: var(--footer-text);
  --color-footer-link: var(--footer-link);
  --color-footer-link-hover: var(--footer-link-hover);
  --color-footer-border: var(--footer-border);
  --font-sans: 'IBM Plex Sans', 'Helvetica Neue', Helvetica, Arial, sans-serif;
  --font-serif: 'IBM Plex Serif', Georgia, 'Times New Roman', serif;
  --font-mono: 'IBM Plex Mono', 'Courier New', Courier, monospace;
}
```

**3. :root light theme tokens** — all hex values from PRD section 2.1 light mode table (copy verbatim from PRD section 2.8 `:root` block).

**4. .dark theme tokens** — all hex values from PRD section 2.1 dark mode table (copy verbatim from PRD section 2.8 `.dark` block).

**5. @font-face declarations** — 9 declarations total with `font-display: swap`:
- IBM Plex Sans: Light (300), Regular (400), Medium (500), SemiBold (600), Bold (700)
- IBM Plex Serif: SemiBold (600), Bold (700)
- IBM Plex Mono: Regular (400), Medium (500)

Each @font-face follows this pattern:
```css
@font-face {
  font-family: 'IBM Plex Sans';
  font-style: normal;
  font-weight: 400;
  font-display: swap;
  src: url('/fonts/IBMPlexSans-Regular.woff2') format('woff2');
}
```

**6. Type scale utility classes** — define as custom CSS classes (NOT in @theme, since these are multi-property utilities). Use `@utility` directive if Tailwind 4 supports it, otherwise use `@layer utilities`:
```css
@layer utilities {
  .text-hero {
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }
  .text-section {
    font-family: var(--font-serif);
    font-size: clamp(1.75rem, 3.5vw, 2.5rem);
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: -0.01em;
  }
  .text-sub {
    font-family: var(--font-sans);
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    font-weight: 600;
    line-height: 1.3;
    letter-spacing: 0;
  }
  .text-body-lg {
    font-family: var(--font-sans);
    font-size: 1.125rem;
    font-weight: 400;
    line-height: 1.6;
    letter-spacing: 0;
  }
  .text-body {
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 400;
    line-height: 1.6;
    letter-spacing: 0;
  }
  .text-caption {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    line-height: 1.4;
    letter-spacing: 0.01em;
  }
  .text-stats {
    font-family: var(--font-mono);
    font-size: clamp(3rem, 6vw, 5rem);
    font-weight: 700;
    line-height: 1.0;
    letter-spacing: -0.02em;
  }
  .text-cite {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 400;
    line-height: 1.4;
    letter-spacing: 0.02em;
  }
}
```

**7. Base body styles:**
```css
body {
  font-family: var(--font-sans);
  background-color: var(--background);
  color: var(--foreground);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```

**8. Scroll indicator keyframe** (from PRD section 6.11):
```css
@keyframes scroll-bob {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(12px); }
}
```
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -c "@font-face" src/styles/global.css && grep -c "@theme" src/styles/global.css && grep -c "text-hero\|text-section\|text-sub\|text-body-lg\|text-body\|text-caption\|text-stats\|text-cite" src/styles/global.css</automated>
  </verify>
  <done>global.css contains: @import "tailwindcss", @theme block with all color and font tokens, :root light tokens, .dark dark tokens, 9 @font-face declarations, 8 type scale utility classes, body styles, scroll-bob keyframe</done>
</task>

<task type="auto">
  <name>Task 2: Create base layout with theme flash prevention and font preloads</name>
  <files>src/layouts/Base.astro, src/pages/index.astro</files>
  <action>
Create `src/layouts/Base.astro` — the base HTML layout used by every page. Include:

**1. Frontmatter:**
```astro
---
interface Props {
  title: string;
  description?: string;
}
const { title, description = 'AI consulting for business leaders. Coaching, training, and fractional executive support.' } = Astro.props;
---
```

**2. HTML structure:**
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="icon" type="image/png" href="/favicon.png" />
  <meta name="generator" content={Astro.generator} />
  <title>{title}</title>
  <meta name="description" content={description} />

  <!-- Theme flash prevention — PRD section 6.10, verbatim -->
  <script is:inline>
    (function() {
      var stored = localStorage.getItem('theme');
      var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      var theme = stored === 'dark' || stored === 'light' ? stored : (stored === 'system' || !stored) ? (prefersDark ? 'dark' : 'light') : 'light';
      if (theme === 'dark') document.documentElement.classList.add('dark');
    })();
  </script>

  <!-- Font preloads — PRD section 2.2 -->
  <link rel="preload" href="/fonts/IBMPlexSans-Regular.woff2" as="font" type="font/woff2" crossorigin />
  <link rel="preload" href="/fonts/IBMPlexSerif-Bold.woff2" as="font" type="font/woff2" crossorigin />
</head>
<body class="bg-background text-foreground font-sans">
  <slot />
</body>
</html>
```

**3. Import global CSS** — add to the frontmatter section:
```astro
import '../styles/global.css';
```

**4. Update `src/pages/index.astro`** to use the Base layout and display a design system test page showing:
- The page title "Consultates" in `text-hero` class
- A subtitle in `text-section` class
- Body text in `text-body` class
- A stat number in `text-stats` class
- A caption in `text-caption` class
- A swatch row showing `bg-primary`, `bg-secondary`, `bg-muted`, `bg-accent` colors
- This test page proves fonts load, tokens work, and theme flash prevention fires

The test page content is temporary — it will be replaced by the real homepage in Phase 3. Its purpose is to visually verify the design system.

IMPORTANT: Use `is:inline` on the theme script so Astro does not bundle/defer it — it must be a blocking inline script.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5 && grep -c "localStorage" src/layouts/Base.astro && grep -c "preload" src/layouts/Base.astro</automated>
  </verify>
  <done>Base.astro renders with: theme flash prevention inline script, 2 font preload tags, global CSS import, favicon link, meta viewport. pnpm build succeeds. Index page renders showing all type scale classes and color tokens.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes with zero errors
- `pnpm dev` serves the test page at localhost:4321 showing correct fonts and colors
- Browser DevTools Network tab shows font files loaded from /fonts/ (no external requests)
- Manually adding `.dark` class to `<html>` switches all tokens to dark mode values
- Page source shows the theme flash prevention script in `<head>` before any other scripts
- All 8 type scale classes (`text-hero` through `text-cite`) render with correct font families and sizes
</verification>

<success_criteria>
- Royal Purple (#5C3B9C light / #8B6CC7 dark) renders as primary color
- IBM Plex Sans, Serif, and Mono load from local WOFF2 files
- Theme flash prevention script fires before paint
- All type scale utility classes apply correct font-family, size, weight, line-height, and letter-spacing
- pnpm build produces zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-02-SUMMARY.md`
</output>
