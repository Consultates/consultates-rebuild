---
phase: 08-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - public/CNAME
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "Pushing to main triggers a GitHub Actions workflow that builds and deploys to GitHub Pages"
    - "The workflow uses Node 22, pnpm 9, and produces static output from dist/"
    - "consultates.com CNAME persists across deployments"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD pipeline for GitHub Pages deployment"
      contains: "pnpm build"
    - path: "public/CNAME"
      provides: "Custom domain configuration"
      contains: "consultates.com"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GitHub Pages"
      via: "actions/deploy-pages"
      pattern: "actions/deploy-pages"
    - from: "public/CNAME"
      to: "dist/CNAME"
      via: "Astro copies public/ to dist/"
      pattern: "consultates.com"
---

<objective>
Create the GitHub Actions CI/CD workflow and GitHub Pages custom domain configuration so that every push to main automatically builds and deploys the Astro site to consultates.com.

Purpose: This is the final phase — the site is fully built and polished, it just needs a deployment pipeline.
Output: Working CI/CD workflow file and CNAME for custom domain.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@astro.config.mjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create `.github/workflows/deploy.yml` with the following configuration:

- **Trigger:** push to `main` branch only
- **Concurrency:** group by `pages`, cancel in-progress deploys
- **Permissions:** `contents: read`, `pages: write`, `id-token: write` (required for GitHub Pages deployment)
- **Environment:** `github-pages` with `url: ${{ steps.deployment.outputs.page_url }}`

Job steps:
1. `actions/checkout@v4`
2. `pnpm/action-setup@v4` with version 9
3. `actions/setup-node@v4` with `node-version: 22` and `cache: 'pnpm'`
4. `pnpm install --frozen-lockfile`
5. `pnpm build`
6. `actions/configure-pages@v5`
7. `actions/upload-pages-artifact@v3` with `path: './dist'`
8. `actions/deploy-pages@v4` with `id: deployment`

The pnpm store is cached via the setup-node cache option (no separate cache step needed).
  </action>
  <verify>
    <automated>cat .github/workflows/deploy.yml && grep -q "pnpm build" .github/workflows/deploy.yml && grep -q "deploy-pages" .github/workflows/deploy.yml && grep -q "node-version: 22" .github/workflows/deploy.yml && echo "PASS"</automated>
  </verify>
  <done>Workflow file exists with correct trigger (push to main), Node 22 + pnpm 9 setup, build step, and GitHub Pages deploy steps</done>
</task>

<task type="auto">
  <name>Task 2: Add CNAME for custom domain and verify build</name>
  <files>public/CNAME</files>
  <action>
Create `public/CNAME` containing exactly one line: `consultates.com` (no trailing newline, no www prefix).

This file lives in `public/` so Astro copies it to `dist/` during build, ensuring the custom domain config persists across deployments.

After creating the file, run `pnpm build` to verify:
1. Build completes without errors
2. `dist/CNAME` exists with correct content
3. `dist/sitemap-index.xml` exists (from @astrojs/sitemap)
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && pnpm build 2>&1 | tail -5 && cat dist/CNAME && test -f dist/sitemap-index.xml && echo "PASS"</automated>
  </verify>
  <done>CNAME file exists in public/ with "consultates.com", build passes cleanly, dist/ contains CNAME and sitemap</done>
</task>

</tasks>

<verification>
1. `.github/workflows/deploy.yml` exists with push-to-main trigger, Node 22, pnpm 9, build, and deploy-pages steps
2. `public/CNAME` contains `consultates.com`
3. `pnpm build` succeeds and `dist/CNAME` is present in output
</verification>

<success_criteria>
- GitHub Actions workflow file is valid YAML with all required steps for Astro + GitHub Pages deployment
- CNAME file ensures consultates.com custom domain persists across deploys
- Build passes locally confirming the pipeline will work in CI
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy/08-01-SUMMARY.md`
</output>
