---
phase: 15-service-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/services/ai-coaching-for-leaders.astro
  - src/pages/services/ai-training-for-teams.astro
  - src/pages/services/fractional-exec-support.astro
autonomous: true
requirements:
  - RDES-51
  - RDES-52
  - RDES-53
  - RDES-54

must_haves:
  truths:
    - "Each of the three service pages has a visually distinct hero and layout from the others"
    - "AI Coaching page shows outcomes as a numbered checklist with left accent markers"
    - "AI Training page shows outcomes as a 2-column icon grid"
    - "Fractional Exec page shows outcomes as a horizontal card row"
    - "Each page has a scenario-based who-it's-for section with persona cards or scenario descriptions"
    - "Each page has an embedded testimonial with photo placeholder circle and attribution"
  artifacts:
    - path: "src/pages/services/ai-coaching-for-leaders.astro"
      provides: "Standalone coaching page with unique layout"
      contains: "text-hero"
    - path: "src/pages/services/ai-training-for-teams.astro"
      provides: "Standalone training page with unique layout"
      contains: "text-hero"
    - path: "src/pages/services/fractional-exec-support.astro"
      provides: "Standalone fractional page with unique layout"
      contains: "text-hero"
  key_links:
    - from: "src/pages/services/ai-coaching-for-leaders.astro"
      to: "src/components/islands/SectionAnimator"
      via: "import for fade-up animations"
      pattern: "SectionAnimator"
    - from: "src/pages/services/ai-training-for-teams.astro"
      to: "src/components/islands/SectionAnimator"
      via: "import for fade-up animations"
      pattern: "SectionAnimator"
    - from: "src/pages/services/fractional-exec-support.astro"
      to: "src/components/islands/SectionAnimator"
      via: "import for fade-up animations"
      pattern: "SectionAnimator"
---

<objective>
Build three distinct service page layouts, each with structured outcomes, scenario-based who-it's-for, and embedded testimonials. Each page stops using the shared ServicePage.astro layout and becomes a standalone page with its own visual identity.

Purpose: The current service pages are identical cookie-cutter layouts. Each service is different -- coaching is intimate 1:1, training is group workshops, fractional is embedded leadership. The layout should reflect that.
Output: Three redesigned service page files, each visually distinct.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/layouts/ServicePage.astro
@src/pages/services/ai-coaching-for-leaders.astro
@src/pages/services/ai-training-for-teams.astro
@src/pages/services/fractional-exec-support.astro
@src/styles/global.css

<interfaces>
<!-- Existing components to reuse -->

From src/components/islands/SectionAnimator.tsx:
```tsx
export function SectionAnimator({ children, delay?: number }: { children: React.ReactNode; delay?: number })
```

From src/components/islands/StakesCTAPulse.tsx:
```tsx
export function StakesCTAPulse({ href, text }: { href: string; text: string })
```

Design system tokens available:
- `--secondary: #F6F2EB` (warm cream)
- `--card-radius: 16px`, `--card-padding: clamp(1.75rem, 3vw, 2.25rem)`
- `--card-shadow`, `--card-shadow-hover`
- `--primary: #5C3B9C` (accent purple)
- `.text-hero`, `.text-section`, `.text-sub`, `.text-body-lg`, `.text-body`, `.text-caption`
- `.glow`, `.corner-marks`, `.btn-alive`, `.btn-alive--on-dark`
- `--section-py`, `--content-max`, `--content-px`
- Standard section padding: `padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem);`
- Social proof pattern: 72x72px circle with `color-mix(in srgb, var(--primary) 10%, transparent)` background, initials in `text-primary`

Testimonial content for service pages (from the homepage social proof pattern):
- Use Frank Wiener quote (adapted/shortened) for Fractional page
- For Coaching and Training pages, use brief placeholder testimonials with initials and titles
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI Coaching page -- intimate 1:1 layout with checklist outcomes</name>
  <files>src/pages/services/ai-coaching-for-leaders.astro</files>
  <action>
Rewrite the AI Coaching page as a standalone page (stop importing ServicePage layout, import Base directly). This page conveys the intimate, personal 1:1 nature of coaching.

**Imports:**
```astro
import Base from '../../layouts/Base.astro';
import { SectionAnimator } from '../../components/islands/SectionAnimator';
import { TIDYCAL_URL } from '../../config';
```

**Section padding constant:** `const sectionPadding = 'padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 5vw, 8rem);';`

**Hero (RDES-51 -- distinct layout: centered, minimal, conversational):**
- Centered text layout (not two-column like About page)
- h1 with `.text-hero` centered, max-width 700px, margin auto
- Subline: `.text-body-lg text-muted-foreground mt-4 text-center`, max-width 600px, margin auto
- Wrap in SectionAnimator client:visible
- Keep existing headline "AI Coaching for Leaders" and subline text exactly

**What You Get (RDES-52 -- numbered checklist with left accent):**
- Section heading: "What you get" with `.text-section mb-8`
- Render as a numbered vertical list with left accent markers:
  - Container: `max-width: 700px` (narrower, intimate feel)
  - Each item: flex row with a numbered circle on the left (32px, `border: 2px solid var(--primary)`, number inside in mono font) and text on the right
  - Items (extract from existing prose, break into concrete outcomes):
    1. "A confidential sounding board" -- "Gary has sat across from C-suite executives for 15 years. The coaching isn't theoretical -- it comes from someone who builds with AI every day."
    2. "Your situation, your agenda" -- "Each session starts with where you are right now. What you're trying to figure out. What's keeping you up at night about AI."
    3. "Specific next steps, not general advice" -- "You leave with actionable steps you can implement immediately. No fixed curriculum -- the agenda is your situation."
  - Each item wrapped in SectionAnimator with staggered delay (0, 0.15, 0.3)
  - Left border on the container: `border-left: 2px solid var(--border)`, items have `padding-left: 1.5rem`

**Who It's For (RDES-53 -- scenario-based):**
- Section heading: "Who it's for" with `.text-section mb-8`
- Render as 2-3 scenario cards in a grid (`grid grid-cols-1 md:grid-cols-3 gap-6`)
- Each card: `background: var(--secondary)`, `border-radius: var(--card-radius)`, `padding: var(--card-padding)`
- Scenarios (derived from existing copy):
  1. Label: "Business Leaders" -- "You're running a company and need clarity on AI without the jargon"
  2. Label: "Founders & Solopreneurs" -- "You're building something and want to use AI as a force multiplier"
  3. Label: "Consultants & Fractionals" -- "You advise others and need to stay ahead of the curve"
- Labels in `.text-sub font-serif mb-2`, descriptions in `.text-body text-muted-foreground`
- Wrap in SectionAnimator

**Testimonial (RDES-54 -- embedded quote):**
- Use cream background section: `background: var(--secondary)`
- Large quotation mark decorative element: `&ldquo;` at 6rem, serif, `text-primary opacity-20`, absolute positioned
- Quote text: "Gary helped me understand exactly where AI fits in my business -- not in theory, but in practice. One session gave me more clarity than months of reading articles." (placeholder -- reasonable for coaching context)
- Attribution: 72x72px circle with `color-mix(in srgb, var(--primary) 10%, transparent)` bg, initials "MR", name "Marketing Director", title "SaaS Company"
- Pattern matches SocialProofSection.astro exactly
- Wrap in SectionAnimator
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>AI Coaching page renders as standalone with centered hero, numbered checklist outcomes, scenario persona cards, and embedded testimonial. No longer uses ServicePage.astro layout.</done>
</task>

<task type="auto">
  <name>Task 2: AI Training + Fractional pages -- distinct layouts with grid and card outcomes</name>
  <files>src/pages/services/ai-training-for-teams.astro, src/pages/services/fractional-exec-support.astro</files>
  <action>
Rewrite both pages as standalone (import Base directly, same imports pattern as Task 1).

## AI Training for Teams (group/workshop feel)

**Hero (RDES-51 -- distinct: left-aligned with a decorative stat callout):**
- Two-column grid on desktop: `grid grid-cols-1 lg:grid-cols-[1fr_300px] gap-12 items-center`
- Left column: h1 `.text-hero` + subline `.text-body-lg text-muted-foreground mt-4`
- Right column: a stat callout card -- `background: var(--secondary)`, `border-radius: var(--card-radius)`, `padding: var(--card-padding)`, centered text
  - Stat number: "30-50" in `.font-serif font-bold` at `clamp(2rem, 4vw, 3rem)`, `color: var(--primary)`
  - Label: "person cohorts trained" in `.text-caption text-muted-foreground mt-2`
- On mobile: single column, stat card below text
- Keep existing headline and subline text

**What You Get (RDES-52 -- 2x2 icon grid):**
- Section heading: "What you get" with `.text-section mb-8`
- 2x2 grid: `grid grid-cols-1 md:grid-cols-2 gap-6`, max-width 900px
- Each cell: card with `background: var(--card)`, `border-radius: var(--card-radius)`, `padding: var(--card-padding)`, `border: 1px solid var(--border)`
- Each card has a small icon area (a 40px circle with `background: color-mix(in srgb, var(--primary) 10%, transparent)`) at top, then heading in `.text-sub font-serif mb-2`, description in `.text-body text-muted-foreground`
- Cards (derived from existing prose):
  1. "Built on real work" -- "Every workshop is built around your team's actual tasks -- the tools they'll use tomorrow, the problems they're dealing with today."
  2. "Learn by doing" -- "People learn by doing, not by watching slides. Your team practises with AI on their actual tasks, not hypothetical examples."
  3. "Behavioural change, not certificates" -- "Gary tracks daily tool usage and behavioural change, not completion rates. The goal is adoption, not attendance."
  4. "Production experience" -- "The curriculum comes from building production systems, not reading about them."
- Stagger with SectionAnimator delays (0, 0.1, 0.2, 0.3)

**Who It's For (RDES-53 -- scenario list with left border):**
- A single card with `border-left: 4px solid var(--primary)`, `background: var(--card)`, `border-radius: var(--card-radius)`, `padding: var(--card-padding)`
- Inside: short scenario descriptions as a vertical list with bullet points (styled `ul` with `space-y-3`)
  - "Teams told to 'use AI' but given no guidance"
  - "Sales, marketing, and operations teams doing knowledge work"
  - "Groups of 5 to 50 people"
- Use `.text-body text-muted-foreground` for items
- Wrap in SectionAnimator

**Testimonial (RDES-54):**
- NOT on cream background (contrast with Coaching page). Use standard white/card background with a top border: `border-top: 1px solid var(--border)`
- Quote: "The workshop transformed how our team thinks about AI. Within a week, three departments had built their own automations." (placeholder)
- Attribution: 72x72px circle, initials "KL", "Head of Operations", "Technology Company"
- Same SectionAnimator pattern

## Fractional Executive Support (authority/leadership feel)

**Hero (RDES-51 -- distinct: dark hero like homepage, commanding presence):**
- Dark background section: `background: #0D1117`
- White text: h1 `.text-hero` with `color: #FFFFFF`
- Subline: `color: rgba(255,255,255,0.7)` with `.text-body-lg mt-4`
- Max-width 800px, centered
- Keep existing headline and subline text
- Wrap in SectionAnimator

**What You Get (RDES-52 -- horizontal card row with serif titles):**
- Section heading: "What you get" with `.text-section mb-8`
- 3-column grid: `grid grid-cols-1 md:grid-cols-3 gap-6`
- Each card: `background: var(--card)`, `border: 1px solid var(--border)`, `border-radius: var(--card-radius)`, `padding: var(--card-padding)`
- Top of each card: a thin accent line (`border-top: 3px solid var(--primary)`)
- Card content (derived from existing prose):
  1. Title: "GTM Strategy" (serif) -- "Building your go-to-market motion from zero across new markets and verticals"
  2. Title: "Product Direction" (serif) -- "Shaping product direction with hands-on leadership, not advisory from a distance"
  3. Title: "Team & Network" (serif) -- "Coaching your team and opening doors through a global network built over 16 countries"
- Titles in `.text-sub font-serif mb-3`, descriptions in `.text-body text-muted-foreground`
- Stagger with SectionAnimator

**Who It's For (RDES-53 -- scenario-based with checkmark list):**
- Section heading: "Who it's for" with `.text-section mb-6`
- Render as a clean checklist (not cards), max-width 700px:
  - Each item: flex row with a small purple check icon (use a simple `span` with `text-primary font-bold` showing a checkmark character `&#10003;`) and text
  - Items:
    - "Startups and scale-ups that need experienced GTM leadership for AI products"
    - "Companies entering new markets or launching AI products"
    - "Teams building their first sales organisation"
    - "Businesses not ready for (or don't need) a full-time exec hire"
- `.text-body text-muted-foreground` for each item, `space-y-4`

**Testimonial (RDES-54):**
- Cream background section like Coaching page pattern: `background: var(--secondary)`
- Use a shortened version of the Frank Wiener quote: "Gary has demonstrated remarkable skill and resilience at introducing a small but innovative start-up into major accounts, competing against well-established Tier 1 incumbents. His strong work ethic, personal networking, and technical prowess have enabled him to enter new markets with minimal support to land key accounts."
- Attribution: 72x72px circle, initials "FW", "Frank Wiener", "CMO and Director of Product Marketing (retired)"
- Same SectionAnimator pattern

After both pages are written, verify the build compiles.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -20 && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <done>AI Training page has left-aligned hero with stat callout, 2x2 icon grid outcomes, bordered scenario list, and testimonial. Fractional page has dark hero, 3-card horizontal outcomes with accent top borders, checkmark scenario list, and cream-bg testimonial with Frank Wiener quote. All three pages are visually distinct from each other. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. Each service page no longer imports ServicePage.astro
3. AI Coaching: centered hero, numbered checklist, persona cards, cream testimonial
4. AI Training: stat callout hero, 2x2 grid, bordered scenario list, bordered testimonial
5. Fractional: dark hero, 3-card row with accent tops, checkmark list, cream testimonial with FW quote
6. All three pages are visually distinct from each other
</verification>

<success_criteria>
Three service pages with distinct layouts (RDES-51), structured outcomes (RDES-52), scenario-based who-it's-for (RDES-53), and embedded testimonials (RDES-54). Build passes. No page looks like another.
</success_criteria>

<output>
After completion, create `.planning/phases/15-service-pages/15-01-SUMMARY.md`
</output>
