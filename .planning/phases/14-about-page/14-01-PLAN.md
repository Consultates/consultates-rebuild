---
phase: 14-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/about.astro
autonomous: true
requirements:
  - RDES-46
  - RDES-47
  - RDES-48
  - RDES-49
  - RDES-50

must_haves:
  truths:
    - "About page hero shows photo on the left and text on the right in a two-column asymmetric layout on desktop"
    - "Career history renders as a vertical timeline with era markers, connecting line, and content blocks"
    - "Family business section is wrapped in a warm-background card with padding and border-radius"
    - "Lead with AI PRO section renders as a distinct card/banner with accent border treatment"
    - "CTA section has dark #0D1117 background with white text, corner marks, glow, and pill button"
    - "On mobile, hero columns stack vertically (photo on top, text below)"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "Complete about page with all 5 redesigned sections"
      contains: "grid.*lg:grid-cols"
  key_links:
    - from: "src/pages/about.astro"
      to: "src/components/islands/StakesCTAPulse"
      via: "import for dark CTA button"
      pattern: "StakesCTAPulse"
    - from: "src/pages/about.astro"
      to: "src/components/islands/SectionAnimator"
      via: "import for fade-up animations"
      pattern: "SectionAnimator"
---

<objective>
Redesign the About page with editorial polish: asymmetric hero, timeline career history, warm family card, Lead with AI PRO banner, and dark CTA section.

Purpose: The about page currently has plain sequential sections with no visual hierarchy. This phase applies the v2.0 design system to create an editorial, polished storytelling layout.
Output: Fully redesigned `src/pages/about.astro` matching all 5 RDES requirements.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/about.astro
@src/styles/global.css

<interfaces>
<!-- Existing components to reuse -->

From src/components/islands/StakesCTAPulse.tsx:
```tsx
// Pill button with pulse glow animation — used in Stakes CTA sections
export function StakesCTAPulse({ href, text }: { href: string; text: string })
```

From src/components/islands/SectionAnimator.tsx:
```tsx
// Simple fade-up on viewport entry wrapper
export function SectionAnimator({ children }: { children: React.ReactNode })
```

From src/components/sections/StakesCTASection.astro:
```astro
<!-- Reference pattern for dark CTA: #0D1117 bg, glow + corner-marks, white h2 with #8B6CC7 em accents -->
<section style="background: #0D1117;">
  <div class="glow corner-marks max-w-[800px] mx-auto text-center" style="--corner-color: rgba(139,108,199,0.4);">
```

Design system tokens available:
- `--secondary: #F6F2EB` (warm cream — for family business card background)
- `--card-radius: 16px`, `--card-padding: clamp(1.75rem, 3vw, 2.25rem)`
- `--card-shadow`, `--card-shadow-hover`
- `--primary: #5C3B9C` (accent border color)
- `.text-hero`, `.text-section`, `.text-body-lg`, `.text-body`, `.text-caption`
- `.glow`, `.corner-marks`, `.btn-alive`, `.btn-alive--on-dark`
- `--section-py`, `--content-max`, `--content-px`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Asymmetric hero + editorial timeline</name>
  <files>src/pages/about.astro</files>
  <action>
Rewrite the About page hero (Section 1) and story (Section 2):

**Hero (RDES-46) — Asymmetric two-column layout:**
- Use CSS grid: `grid grid-cols-1 lg:grid-cols-[280px_1fr] gap-12 items-center`
- Left column: photo (200x200 rounded-full, keep existing src/alt/width/height)
- Right column: h1 with `.text-hero`, subline with `.text-body-lg text-muted-foreground mt-4`
- On mobile (default): single column, photo centered above text
- On desktop (lg): photo left, text right — asymmetric feel
- Use design system spacing: `padding: var(--section-py) var(--content-px)`, `max-width: var(--content-max)`, `margin: 0 auto`

**Story / Timeline (RDES-47) — Editorial timeline layout:**
- Section heading stays: "The through-line" with `.text-section mb-12`
- Convert the 5 paragraphs into timeline entries with era markers
- Timeline structure: a vertical line on the left (2px wide, `bg-border` color, absolute positioned) with era markers (small circles or year labels) at each entry
- Each entry is a flex row: left column = era marker (a small dot or short label like "1980s", "1990s", "2000s", "2010s", "Today"), right column = paragraph text
- Era markers derived from content:
  1. "1980s" — Started coding at 10, joined RAF (military electronics paragraph)
  2. "1990s" — Telecoms/internet infrastructure, Juniper Networks, Cyan (infrastructure paragraph)
  3. "2000s" — Engineering to sales transition, VP Sales, CRO, 16 countries (sales paragraph)
  4. "Today" — Ships production AI apps, builds solo (current work paragraph)
  5. "Why it matters" — The differentiator paragraph (not all eras, a summary)
- Timeline CSS: relative container, `::before` pseudo-element for the vertical line, positioned left at ~60px, full height, 2px wide, `background: var(--border)`
- Each entry: `padding-left: 80px` with the dot/marker absolutely positioned at left ~52px
- Dot: 12px circle, `border: 2px solid var(--primary)`, `background: var(--background)`, positioned vertically centered with the first line of text
- Use `SectionAnimator` (client:visible) wrapping each timeline entry for staggered fade-up
- On mobile: same vertical timeline, slightly tighter padding-left (~60px), marker at ~24px

Keep all paragraph text exactly as-is — do not change any copy.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>Hero displays as asymmetric two-column on desktop and stacked on mobile. Career history renders as a vertical editorial timeline with era markers, connecting line, and all 5 original paragraphs preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Family card + AI PRO banner + dark CTA</name>
  <files>src/pages/about.astro</files>
  <action>
Continue editing about.astro — restyle Sections 3, 4, and 5:

**Family Business card (RDES-48) — Warm-background card:**
- Wrap content in a card div: `background: var(--secondary)` (cream #F6F2EB), `border-radius: var(--card-radius)` (16px), `padding: var(--card-padding)`
- Add subtle `box-shadow: var(--card-shadow)`
- Keep heading "A family business" as `.text-section mb-6` inside the card
- Keep paragraph text as-is with `.text-body text-secondary-foreground`
- Card sits within the standard section container (`max-width: var(--content-max)`, centered)
- Wrap in SectionAnimator for fade-up entrance

**Lead with AI PRO banner (RDES-49) — Distinct card/banner:**
- Different visual treatment from family business card to create contrast
- Use a left accent border: `border-left: 4px solid var(--primary)` (#5C3B9C)
- Background: `var(--card)` (white in light, dark card in dark mode)
- Border-radius: `var(--card-radius)`, padding: `var(--card-padding)`
- Add `box-shadow: var(--card-shadow)`
- Heading "Lead with AI PRO" as `.text-section mb-6`
- Paragraph as `.text-body text-muted-foreground`
- Wrap in SectionAnimator for fade-up entrance

**Dark CTA section (RDES-50) — Matching homepage Stakes CTA pattern:**
- Import `StakesCTAPulse` from `../components/islands/StakesCTAPulse` and `SectionAnimator` from `../components/islands/SectionAnimator`
- Section background: `background: #0D1117`
- Inner div: `class="glow corner-marks"` with `max-w-[800px] mx-auto text-center`, padding same as hero section
- Set `--corner-color: rgba(139,108,199,0.4)` inline
- Heading: `<h2 class="text-section" style="color: #FFFFFF;">` with a compelling CTA line. Use existing copy pattern: "Ready to talk?" or similar — keep it brief. Wrap a key word in `<em style="font-style: normal; color: #8B6CC7;">` for accent
- Below heading: `<StakesCTAPulse client:visible href={TIDYCAL_URL} text="Book a Free Call" />`
- Wrap heading in `<SectionAnimator client:visible>`
- Replace the existing plain Button CTA section entirely

Ensure the frontmatter imports are updated: add `StakesCTAPulse` and `SectionAnimator` imports. Keep existing `Base`, `Button` (can remove if no longer used), and `TIDYCAL_URL` imports.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && npx astro check 2>&1 | tail -20 && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <done>Family business section wrapped in warm cream card. Lead with AI PRO section styled as accent-bordered card distinct from family card. CTA section uses dark #0D1117 background with glow, corner marks, white text, em accents, and StakesCTAPulse pill button. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. About page at `/about` renders all 5 sections with redesigned layout
3. Hero: two-column on desktop, stacked on mobile
4. Timeline: vertical line with era markers and 5 content entries
5. Family card: cream background, rounded corners
6. AI PRO card: accent left border, card background
7. CTA: dark background with glow, corners, pill button
</verification>

<success_criteria>
All 5 RDES requirements (46-50) implemented in a single about.astro file. Build passes. Visual structure matches the editorial polish described in the phase goal.
</success_criteria>

<output>
After completion, create `.planning/phases/14-about-page/14-01-SUMMARY.md`
</output>
