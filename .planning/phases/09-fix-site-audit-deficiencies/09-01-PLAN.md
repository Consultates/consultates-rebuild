---
phase: 09-fix-site-audit-deficiencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Base.astro
  - src/components/Footer.astro
  - src/components/sections/TrustBar.astro
  - src/pages/about.astro
autonomous: true
requirements:
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-07
  - AUDIT-08
  - AUDIT-09
  - AUDIT-10
  - AUDIT-11
  - AUDIT-12
  - AUDIT-13

must_haves:
  truths:
    - "No JS errors in browser console on any non-homepage page"
    - "Footer shows info@consultates.com, company LinkedIn URL, correct location, correct quote, correct copyright"
    - "Footer Services link navigates to a real page (not /services 404)"
    - "Footer Privacy Policy link navigates to /privacy-policy (not /privacy 404)"
    - "TrustBar has id=section-trust in the DOM"
    - "About page 'Most AI consultants...' paragraph is not styled as a blockquote"
  artifacts:
    - path: "src/layouts/Base.astro"
      provides: "GSAP guard preventing crash on pages without .scroll-section"
      contains: "if.*sections\\.length.*===.*0.*return"
    - path: "src/components/Footer.astro"
      provides: "Corrected footer content matching PRD"
      contains: "info@consultates.com"
    - path: "src/components/sections/TrustBar.astro"
      provides: "Trust bar with section-trust ID"
      contains: "section-trust"
    - path: "src/pages/about.astro"
      provides: "About page with correct paragraph rendering"
  key_links:
    - from: "src/components/Footer.astro"
      to: "/privacy-policy"
      via: "href attribute"
      pattern: "href.*privacy-policy"
    - from: "src/components/Footer.astro"
      to: "/services/ai-coaching-for-leaders"
      via: "href attribute"
      pattern: "href.*services/ai-coaching"
---

<objective>
Fix all non-homepage audit deficiencies: GSAP crash guard on inner pages, all 7 footer content errors, missing section-trust ID on TrustBar, and about page quote rendering.

Purpose: Eliminate JS errors on every inner page and correct wrong content/links in the global footer.
Output: Four corrected files — Base.astro, Footer.astro, TrustBar.astro, about.astro.
</objective>

<execution_context>
@/home/agenttate/.claude/get-shit-done/workflows/execute-plan.md
@/home/agenttate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@audit/SITE-AUDIT.md
@.planning/phases/09-fix-site-audit-deficiencies/09-CONTEXT.md

<interfaces>
<!-- Key files being modified — current state for reference -->

From src/layouts/Base.astro (line 72-80 — GSAP script):
```javascript
if (isDesktop && !prefersReducedMotion) {
  const sections = gsap.utils.toArray<HTMLElement>('.scroll-section');
  // ... line 80:
  const container = sections[0].parentElement!;
```
Fix target: Add guard `if (sections.length === 0) return;` after toArray, before accessing sections[0].

From src/components/Footer.astro (lines 4-11 — quickLinks):
```javascript
const quickLinks = [
  { href: '/', label: 'Home' },
  { href: '/services', label: 'Services' },        // 404 — fix
  { href: '/about', label: 'About' },
  { href: '/blog', label: 'Blog' },
  { href: '/contact', label: 'Contact' },
  { href: '/privacy', label: 'Privacy Policy' },    // 404 — fix
];
```

From src/components/sections/TrustBar.astro:
```html
<p class="absolute bottom-2 left-1/2 ...">
```
Needs wrapping element with `id="section-trust"`.

From src/pages/about.astro (line 33 — the "Most AI consultants..." paragraph):
Currently a regular `<p>` tag. Audit says it renders as quoted text — check for errant styling.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GSAP crash guard and footer corrections</name>
  <files>src/layouts/Base.astro, src/components/Footer.astro</files>
  <action>
In `src/layouts/Base.astro`, add an early return guard inside the `if (isDesktop && !prefersReducedMotion)` block, immediately after the `const sections = gsap.utils.toArray<HTMLElement>('.scroll-section');` line (line 72). Add: `if (sections.length === 0) return;` — this prevents the crash on all non-homepage pages where no `.scroll-section` elements exist.

In `src/components/Footer.astro`, make these 7 corrections:

1. **Services link (Issue #4):** Change `{ href: '/services', label: 'Services' }` to `{ href: '/services/ai-coaching-for-leaders', label: 'Services' }` — there is no /services index page; link to the first service page.

2. **Privacy Policy link (Issue #5):** Change `{ href: '/privacy', label: 'Privacy Policy' }` to `{ href: '/privacy-policy', label: 'Privacy Policy' }`.

3. **Email (Issue #7):** Change both the `href="mailto:gary@consultates.com"` and the display text `gary@consultates.com` to `info@consultates.com`.

4. **LinkedIn (Issue #8):** Change `href="https://linkedin.com/in/garyctate"` to `href="https://www.linkedin.com/company/consultates-limited/"`.

5. **Location text (Issue #9):** Change `London, UK &middot; Saigon, Vietnam &middot; Everywhere online` to `Global and Remote — Connecting where you are` (use an em dash `&mdash;`).

6. **Quote (Issue #10):** Change `"We help good people in a turbulent time"` to `"Artificial intelligence is not a substitute for human intelligence; it is a tool to amplify human creativity and ingenuity."`.

7. **Copyright (Issue #11):** Change `&copy; {year} Consultates Limited. All rights reserved.` to `&copy; {year} Consultates Limited` (remove ". All rights reserved.").
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -q "sections.length === 0" src/layouts/Base.astro && grep -q "info@consultates.com" src/components/Footer.astro && grep -q "privacy-policy" src/components/Footer.astro && grep -q "ai-coaching-for-leaders" src/components/Footer.astro && grep -q "company/consultates-limited" src/components/Footer.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Base.astro has GSAP guard — no JS errors on inner pages. Footer.astro has all 7 content corrections matching PRD exactly.</done>
</task>

<task type="auto">
  <name>Task 2: TrustBar section ID and About page quote fix</name>
  <files>src/components/sections/TrustBar.astro, src/pages/about.astro</files>
  <action>
In `src/components/sections/TrustBar.astro`:
- Wrap the existing `<p>` element in a `<div id="section-trust">` container. The `<p>` keeps its existing classes. The wrapping div needs no additional styling — it just provides the anchor ID.

In `src/pages/about.astro`:
- The "Most AI consultants..." paragraph (line 33) is the last `<p>` inside the "The through-line" section's `.space-y-4` div. The audit says it renders as quoted text. Inspect the actual content — the paragraph text starts with "Most AI consultants fall into one camp:" which could trigger some prose styling if a parent applies typography plugin rules. The fix: ensure this paragraph has no blockquote ancestor and has the same `class="text-body text-muted-foreground"` as sibling paragraphs. If there's any `>` character or `<blockquote>` wrapper, remove it. The content should remain as a plain `<p>` tag matching the other paragraphs in that section.

Note: Looking at the source, all paragraphs in that section already have identical `class="text-body text-muted-foreground"` and are plain `<p>` tags. The "quoted text" appearance may come from the content itself being opinionated/quotable, or the audit may have detected quotation marks in the rendered text. Verify the rendered output — if the paragraph is already a plain `<p>` with no blockquote wrapper, the issue may be a false positive from the audit. In that case, no change is needed to about.astro, but document this finding.
  </action>
  <verify>
    <automated>cd /home/agenttate/.openclaw/Mi6-builds/consultates-rebuild && grep -q 'id="section-trust"' src/components/sections/TrustBar.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TrustBar has `id="section-trust"` in the DOM. About page "Most AI consultants..." paragraph renders as regular text, not a blockquote.</done>
</task>

</tasks>

<verification>
1. Run `pnpm build` — no build errors
2. Run dev server, open browser console on `/about`, `/contact`, `/services/ai-coaching-for-leaders` — no JS errors
3. Check footer on any page: email is info@consultates.com, LinkedIn points to company page, Privacy Policy links to /privacy-policy, Services links to /services/ai-coaching-for-leaders, location reads "Global and Remote", quote is the AI/human intelligence quote, copyright has no "All rights reserved"
4. DOM inspect homepage: TrustBar element has `id="section-trust"`
</verification>

<success_criteria>
- Zero JS console errors on all non-homepage pages
- All 7 footer content values match PRD exactly
- `#section-trust` exists in homepage DOM
- About page paragraph is plain text, not blockquote
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-site-audit-deficiencies/09-01-SUMMARY.md`
</output>
