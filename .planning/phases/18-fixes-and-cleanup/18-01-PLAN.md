---
phase: 18-fixes-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Nav.astro
  - src/components/FormInput.astro
  - src/content/blog/dummy-strategy-first-steps.md
  - src/content/blog/dummy-tools-prompt-engineering.md
  - src/content/blog/dummy-ai-safety-boardroom.md
autonomous: true

must_haves:
  truths:
    - "Home nav dropdown (desktop) shows Stakes entry with Warning icon, label 'Stakes', and desc 'What you risk by waiting' — clicking it navigates to section index 1"
    - "Home nav dropdown (mobile) shows Stakes entry — tapping it navigates to section index 1 on homepage or links to /?section=1 on other pages"
    - "Contact form text inputs with icons (name, email, company) have left padding so typed text does not overlap the icon"
    - "No dummy blog posts exist in src/content/blog/ — only 3 real posts remain"
    - "Blog carousel on homepage and blog index renders correctly with 3 posts"
  artifacts:
    - path: "src/components/Nav.astro"
      provides: "Stakes section in homeSections array + Warning icon SVG in desktop dropdown"
      contains: "warning"
    - path: "src/components/FormInput.astro"
      provides: "Icon padding applied to input element"
      contains: "iconPaddingClass"
  key_links:
    - from: "Nav.astro homeSections array"
      to: "Base.astro scrollToSection(1)"
      via: "data-section-index=1 on button, /?section=1 on anchor"
      pattern: "index: 1"
    - from: "FormInput.astro iconPaddingClass"
      to: "input element class attribute"
      via: "class:list merging fieldClasses and iconPaddingClass"
      pattern: "class:list.*iconPaddingClass"
---

<objective>
Fix three known bugs and remove test content so the live site has no visible defects.

Purpose: Clean baseline before homepage polish, footer redesign, and integrations work in subsequent phases.
Output: Nav.astro with Stakes entry, FormInput.astro with icon padding fix, dummy blog posts deleted.
</objective>

<execution_context>
@/Users/garyctate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/garyctate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-fixes-and-cleanup/18-RESEARCH.md
@src/components/Nav.astro
@src/components/FormInput.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stakes section to Home nav dropdown</name>
  <files>src/components/Nav.astro</files>
  <action>
  1. Add Stakes entry as the FIRST item in the `homeSections` array (line 35-42):
     ```
     { index: 1, label: 'Stakes', desc: 'What you risk by waiting', icon: 'warning' },
     ```
     This must be the first entry (before index 2) so the dropdown order matches the homepage scroll order.

  2. Add a Phosphor Warning duotone SVG conditional block in the desktop dropdown icon section (lines 102-137 pattern). Insert it BEFORE the `squares-four` block. Use this exact SVG markup:
     ```
     {section.icon === 'warning' && (
       <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 256 256" class="shrink-0 text-primary">
         <path d="M236.73,187.89,149.13,36.22h0a24.34,24.34,0,0,0-42.26,0L19.27,187.89A23.51,23.51,0,0,0,18,200.55,24.3,24.3,0,0,0,40.87,224H215.13A24.3,24.3,0,0,0,238,200.55,23.51,23.51,0,0,0,236.73,187.89Z" opacity="0.2"></path>
         <path d="M236.8,188.09,149.35,36.22h0a24.76,24.76,0,0,0-42.7,0L19.2,188.09a23.51,23.51,0,0,0,0,23.72A24.35,24.35,0,0,0,40.55,224h174.9a24.35,24.35,0,0,0,21.33-12.19A23.51,23.51,0,0,0,236.8,188.09ZM222.93,203.8a8.5,8.5,0,0,1-7.48,4.2H40.55a8.5,8.5,0,0,1-7.48-4.2,7.59,7.59,0,0,1,0-7.72L120.52,44.21a8.75,8.75,0,0,1,15,0l87.45,151.87A7.59,7.59,0,0,1,222.93,203.8ZM120,144V104a8,8,0,0,1,16,0v40a8,8,0,0,1-16,0Zm20,36a12,12,0,1,1-12-12A12,12,0,0,1,140,180Z"></path>
       </svg>
     )}
     ```

  3. The mobile menu (line 273) already iterates `homeSections` — it will automatically pick up the new entry. No changes needed there.

  4. Cross-page nav: The mobile menu uses `/?section=${section.index}` for non-homepage links. Index 1 will correctly map to Stakes on homepage via Base.astro's URL param reader.
  </action>
  <verify>
  Run `pnpm build` — no errors. Grep Nav.astro for `index: 1` and `warning` to confirm both the array entry and SVG block exist.
  </verify>
  <done>
  Desktop and mobile Home dropdown both show "Stakes" as the first section item. On homepage, clicking it calls scrollToSection(1). On other pages, it links to /?section=1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix FormInput icon padding bug</name>
  <files>src/components/FormInput.astro</files>
  <action>
  On line 62, change the input element's class attribute from:
  ```
  class={fieldClasses}
  ```
  to:
  ```
  class:list={[fieldClasses, iconPaddingClass]}
  ```

  This applies the `pl-10` padding class when an icon is present, preventing typed text from overlapping the icon. The `iconPaddingClass` is already computed at line 36 — it returns `'pl-10'` when `icon` is truthy and `textarea` is falsy, or `''` otherwise.

  Using `class:list` instead of template literal concatenation avoids a trailing space when `iconPaddingClass` is empty string.

  Do NOT change the textarea element (line 52) — textareas don't have icons in the current design.
  </action>
  <verify>
  Run `pnpm build` — no errors. Grep FormInput.astro for `class:list.*iconPaddingClass` to confirm the fix.
  </verify>
  <done>
  Contact form text inputs with icons (name/user, email/envelope, company/buildings) have `pl-10` padding so typed text never overlaps the icon. Textarea (message) is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove dummy blog posts</name>
  <files>
    src/content/blog/dummy-strategy-first-steps.md
    src/content/blog/dummy-tools-prompt-engineering.md
    src/content/blog/dummy-ai-safety-boardroom.md
  </files>
  <action>
  1. Search the entire codebase for references to dummy post slugs before deleting:
     - `dummy-strategy-first-steps`
     - `dummy-tools-prompt-engineering`
     - `dummy-ai-safety-boardroom`
     If any file references these slugs (other than the posts themselves), remove those references too.

  2. Delete the three dummy blog post files:
     ```
     rm src/content/blog/dummy-strategy-first-steps.md
     rm src/content/blog/dummy-tools-prompt-engineering.md
     rm src/content/blog/dummy-ai-safety-boardroom.md
     ```

  3. After deletion, verify 3 real posts remain:
     - youre-not-the-only-one-stuck.md
     - how-safe-is-ai-really.md
     - get-out-of-the-ai-cage.md

  4. Run `pnpm build` to confirm the blog carousel and blog index render without errors with only 3 posts. The BlogCarouselIsland handles any post count — no minimum required.

  Do NOT remove any category values from the content collection schema — empty categories are harmless and may be used by future real posts.
  </action>
  <verify>
  Run `ls src/content/blog/` — should show exactly 3 files (no dummy- prefix). Run `pnpm build` — no errors.
  </verify>
  <done>
  Only 3 real blog posts exist. Blog index and homepage blog carousel render correctly. No dummy/test content visible on the live site.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. `grep -c 'index: 1' src/components/Nav.astro` returns at least 1
3. `grep -c 'warning' src/components/Nav.astro` returns at least 1
4. `grep 'iconPaddingClass' src/components/FormInput.astro` shows it used in class:list on the input element
5. `ls src/content/blog/dummy-* 2>/dev/null` returns nothing (no dummy posts)
6. `ls src/content/blog/*.md | wc -l` returns 3
</verification>

<success_criteria>
- Home nav dropdown shows Stakes section with Warning icon at position 1 — navigates to NegativeStakes section
- Contact form inputs with icons have left padding (pl-10) — text never overlaps icons
- No dummy blog posts remain — only 3 real posts in content directory
- Site builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-fixes-and-cleanup/18-01-SUMMARY.md`
</output>
